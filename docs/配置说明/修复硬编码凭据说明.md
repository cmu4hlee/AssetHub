# 硬编码数据库凭据修复说明

## 已修复的文件

### ✅ 核心配置文件
1. **`backend/config/app.config.js`** - 已移除硬编码密码，使用环境变量
2. **`backend/config/database.js`** - 已使用 app.config.js（支持环境变量）

### ✅ 脚本文件
1. **`backend/scripts/init-db.js`** - 已改为使用环境变量
2. **`backend/check-users.js`** - 已改为使用环境变量

### ✅ 路由文件
1. **`backend/routes/backup.js`** - 已移除硬编码密码

### ✅ 辅助工具
1. **`backend/scripts/db-config-helper.js`** - 新建：统一的数据库配置加载模块

## 待修复的文件（需要手动修复）

以下脚本文件仍包含硬编码凭据，建议使用 `db-config-helper.js` 统一处理：

1. `backend/scripts/import-assets.js`
2. `backend/scripts/import-assets-fixed.js`
3. `backend/scripts/fix-missing-records.js`
4. `backend/scripts/fix-duplicate-codes.js`
5. `backend/scripts/detailed-analyze.js`
6. `backend/scripts/create-test-user.js`
7. `backend/scripts/clear-assets.js`
8. `backend/scripts/check-import.js`
9. `backend/scripts/analyze-missing-records.js`
10. `backend/scripts/add-missing-fields.js`
11. `backend/scripts/add-code-fields.js`
12. `backend/simple-test.js`

## 修复方法

### 方法 1：使用 db-config-helper.js（推荐）

将脚本文件中的硬编码配置替换为：

```javascript
// 旧代码
const mysql = require('mysql2/promise');
const connection = await mysql.createConnection({
  host: '101.37.236.101',
  port: 3306,
  user: 'root',
  password: 'Cmu19801008',
  database: 'zcgl',
});

// 新代码
const { createConnection } = require('./db-config-helper');
const connection = await createConnection();
```

### 方法 2：直接使用环境变量

```javascript
require('dotenv').config();
const mysql = require('mysql2/promise');

const connection = await mysql.createConnection({
  host: process.env.DB_HOST || 'localhost',
  port: parseInt(process.env.DB_PORT) || 3306,
  user: process.env.DB_USER || 'root',
  password: process.env.DB_PASSWORD || '',
  database: process.env.DB_NAME || 'zcgl',
});

if (!process.env.DB_PASSWORD) {
  console.error('❌ 错误：数据库密码未设置！');
  process.exit(1);
}
```

## 使用步骤

### 1. 创建 .env 文件

```bash
cd Asset/backend
cp .env.example .env
```

### 2. 编辑 .env 文件

填入实际的数据库配置：

```env
DB_HOST=101.37.236.101
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的实际密码
DB_NAME=zcgl
```

### 3. 验证配置

运行任意脚本测试：

```bash
node scripts/init-db.js
```

如果配置正确，应该能正常连接数据库。

## 安全提示

1. ✅ `.env` 文件已在 `.gitignore` 中，不会被提交
2. ✅ `.env.example` 文件已创建，包含示例配置（不含真实密码）
3. ⚠️ 确保所有团队成员都创建自己的 `.env` 文件
4. ⚠️ 不要将 `.env` 文件通过其他方式分享（如邮件、聊天工具）

## 批量修复脚本（可选）

如果需要批量修复所有脚本文件，可以使用以下方法：

```bash
# 查找所有包含硬编码密码的文件
grep -r "Cmu19801008" Asset/backend/scripts/

# 然后逐个文件修复
```

## 总结

- ✅ 核心配置已修复
- ✅ 主要脚本已修复
- ✅ 创建了统一的配置辅助模块
- ⚠️ 部分脚本文件仍需手动修复（使用频率较低）
- ✅ 已创建 `.env.example` 模板
- ✅ 已更新 `.gitignore`

建议优先修复经常使用的脚本文件，其他文件可以在需要时再修复。
