# AssetHost智能资产管理平台 源代码
**版本：** V1.0

---

## 第 1 页

   1  // ===== FILE: backend/adapters/asset-module-adapter.js =====

   2  /**

   3   * 资产模块接口适配器

   4   *

   5   * 将现有资产服务适配为标准接口，实现模块独立性。

   6   */

   7  

   8  const db = require('../config/database');

   9  const logger = require('../config/logger');

  10  const cacheService = require('../services/cache.service');

  11  const { safeExecuteWithRetry, createModuleError } = require('../middleware/module-error-handler');

  12  const moduleRegistry = require('../config/module-registry');

  13  

  14  class AssetModuleAdapter {

  15    constructor() {

  16      this.name = 'asset';

  17      this.version = '1.0.0';

  18      this.config = null;

  19      this.initialized = false;

  20    }

  21  

  22    /**

  23     * 模块初始化

  24     *

  25     * @param {Object} config - 模块配置

  26     */

  27    async initialize(config = {}) {

  28      this.config = {

  29        cacheEnabled: true,

  30        cacheTTL: 300,

  31        maxPageSize: 2000,

  32        defaultPageSize: 20,

  33        ...config,

  34      };

  35      this.initialized = true;

  36      logger.info('资产模块初始化完成');

  37    }

  38  

  39    /**

  40     * 根据ID查找资产

  41     *

  42     * @param {number} id - 资产ID

  43     * @param {number} tenantId - 租户ID

  44     * @returns {Promise<Object|null>}

  45     */

  46    async findById(id, tenantId) {

  47      const cacheKey = `asset:${tenantId}:${id}`;

  48      const cached = await safeExecuteWithRetry(

  49        () => cacheService.get(cacheKey),

  50        1,


*第 1 页*

---

## 第 2 页

   1        1000,

   2        null,

   3      );

   4      if (cached) return cached;

   5  

   6      const [rows] = await db.execute(

   7        'SELECT * FROM assets WHERE id = ? AND tenant_id = ? AND is_deleted = 0',

   8        [id, tenantId],

   9      );

  10  

  11      if (rows.length === 0) return null;

  12  

  13      await safeExecuteWithRetry(

  14        () => cacheService.set(cacheKey, rows[0], this.config.cacheTTL),

  15        1,

  16        1000,

  17        null,

  18      );

  19  

  20      return rows[0];

  21    }

  22  

  23    /**

  24     * 查询资产列表

  25     *

  26     * @param {Object} filters - 过滤条件

  27     * @param {number} tenantId - 租户ID

  28     * @param {Object} pagination - 分页参数

  29     * @returns {Promise<{data: Array, total: number}>}

  30     */

  31    async findAll(filters, tenantId, pagination = {}) {

  32      const { page = 1, pageSize = this.config.defaultPageSize } = pagination;

  33      const offset = (page - 1) * pageSize;

  34  

  35      let whereClause = 'WHERE a.tenant_id = ? AND a.is_deleted = 0';

  36      const params = [tenantId];

  37  

  38      if (filters.status) {

  39        whereClause += ' AND a.status = ?';

  40        params.push(filters.status);

  41      }

  42      if (filters.department) {

  43        whereClause += ' AND a.department_new = ?';

  44        params.push(filters.department);

  45      }

  46      if (filters.categoryId) {

  47        whereClause += ' AND a.category_id = ?';

  48        params.push(filters.categoryId);

  49      }

  50      if (filters.keyword) {


*第 2 页*

---

## 第 3 页

   1        whereClause += ' AND (a.asset_code LIKE ? OR a.name LIKE ?)';

   2        params.push(`%${filters.keyword}%`, `%${filters.keyword}%`);

   3      }

   4  

   5      const [countResult] = await db.execute(

   6        `SELECT COUNT(*) as total FROM assets a ${whereClause}`,

   7        params,

   8      );

   9  

  10      const [rows] = await db.execute(

  11        `SELECT a.* FROM assets a ${whereClause} ORDER BY a.created_at DESC LIMIT ? OFFSET ?`,

  12        [...params, pageSize, offset],

  13      );

  14  

  15      return {

  16        data: rows,

  17        total: countResult[0].total,

  18        page,

  19        pageSize,

  20      };

  21    }

  22  

  23    /**

  24     * 创建资产

  25     *

  26     * @param {Object} data - 资产数据

  27     * @param {number} tenantId - 租户ID

  28     * @param {string} createdBy - 创建人

  29     * @returns {Promise<Object>}

  30     */

  31    async create(data, tenantId, createdBy) {

  32      const { asset_code, name, category_id, purchase_price, status, department_new } = data;

  33  

  34      const [result] = await db.execute(

  35        `INSERT INTO assets (tenant_id, asset_code, name, category_id, purchase_price, status, department_new, created_by, created_at)

  36         VALUES (?, ?, ?, ?, ?, ?, ?, ?, NOW())`,

  37        [tenantId, asset_code, name, category_id, purchase_price, status || '闲置', department_new, createdBy],

  38      );

  39  

  40      const asset = await this.findById(result.insertId, tenantId);

  41  

  42      // 触发事件

  43      moduleRegistry.emitEvent('asset', 'created', {

  44        id: result.insertId,

  45        assetCode: asset_code,

  46        tenantId,

  47        createdBy,

  48      });

  49  

  50      return asset;


*第 3 页*

---

## 第 4 页

   1    }

   2  

   3    /**

   4     * 更新资产

   5     *

   6     * @param {number} id - 资产ID

   7     * @param {Object} data - 更新数据

   8     * @param {number} tenantId - 租户ID

   9     * @param {string} updatedBy - 更新人

  10     * @returns {Promise<Object>}

  11     */

  12    async update(id, data, tenantId, updatedBy) {

  13      const existing = await this.findById(id, tenantId);

  14      if (!existing) {

  15        throw createModuleError('NOT_FOUND', `资产 ${id} 不存在`);

  16      }

  17  

  18      const updates = [];

  19      const values = [];

  20      const allowedFields = ['name', 'status', 'department_new', 'location', 'category_id', 'purchase_price', 'current_value'];

  21  

  22      for (const [key, value] of Object.entries(data)) {

  23        if (allowedFields.includes(key) && value !== undefined) {

  24          updates.push(`${key} = ?`);

  25          values.push(value);

  26        }

  27      }

  28  

  29      updates.push('updated_at = NOW()');

  30      updates.push('updated_by = ?');

  31      values.push(updatedBy);

  32      values.push(id);

  33      values.push(tenantId);

  34  

  35      await db.execute(

  36        `UPDATE assets SET ${updates.join(', ')} WHERE id = ? AND tenant_id = ?`,

  37        values,

  38      );

  39  

  40      // 清除缓存

  41      await safeExecuteWithRetry(

  42        () => cacheService.deleteByPattern(`asset:${tenantId}:${id}`),

  43        1,

  44        1000,

  45        null,

  46      );

  47  

  48      const asset = await this.findById(id, tenantId);

  49  

  50      // 触发事件


*第 4 页*

---

## 第 5 页

   1      moduleRegistry.emitEvent('asset', 'updated', {

   2        id,

   3        tenantId,

   4        updatedBy,

   5        changes: data,

   6      });

   7  

   8      return asset;

   9    }

  10  

  11    /**

  12     * 删除资产（软删除）

  13     *

  14     * @param {number} id - 资产ID

  15     * @param {number} tenantId - 租户ID

  16     * @returns {Promise<boolean>}

  17     */

  18    async delete(id, tenantId) {

  19      const existing = await this.findById(id, tenantId);

  20      if (!existing) {

  21        throw createModuleError('NOT_FOUND', `资产 ${id} 不存在`);

  22      }

  23  

  24      if (existing.status === '在用') {

  25        throw createModuleError('CONFLICT', '在用资产不能删除');

  26      }

  27  

  28      await db.execute(

  29        'UPDATE assets SET is_deleted = 1, deleted_at = NOW() WHERE id = ? AND tenant_id = ?',

  30        [id, tenantId],

  31      );

  32  

  33      // 清除缓存

  34      await safeExecuteWithRetry(

  35        () => cacheService.deleteByPattern(`asset:${tenantId}:${id}`),

  36        1,

  37        1000,

  38        null,

  39      );

  40  

  41      // 触发事件

  42      moduleRegistry.emitEvent('asset', 'deleted', {

  43        id,

  44        tenantId,

  45        assetCode: existing.asset_code,

  46      });

  47  

  48      return true;

  49    }

  50  


*第 5 页*

---

## 第 6 页

   1    /**

   2     * 统计资产数量

   3     *

   4     * @param {Object} filters - 过滤条件

   5     * @param {number} tenantId - 租户ID

   6     * @returns {Promise<number>}

   7     */

   8    async count(filters, tenantId) {

   9      let whereClause = 'WHERE tenant_id = ? AND is_deleted = 0';

  10      const params = [tenantId];

  11  

  12      if (filters.status) {

  13        whereClause += ' AND status = ?';

  14        params.push(filters.status);

  15      }

  16  

  17      const [result] = await db.execute(

  18        `SELECT COUNT(*) as total FROM assets ${whereClause}`,

  19        params,

  20      );

  21  

  22      return result[0].total;

  23    }

  24  

  25    /**

  26     * 模块启动

  27     */

  28    async start() {

  29      logger.info('资产模块已启动');

  30    }

  31  

  32    /**

  33     * 模块停止

  34     */

  35    async stop() {

  36      logger.info('资产模块已停止');

  37    }

  38  

  39    /**

  40     * 模块销毁

  41     */

  42    async destroy() {

  43      this.initialized = false;

  44      logger.info('资产模块已销毁');

  45    }

  46  }

  47  

  48  module.exports = AssetModuleAdapter;

  49  

  50  


*第 6 页*

---

## 第 7 页

   1  // ===== FILE: backend/adapters/quality-control-module-adapter.js =====

   2  /**

   3   * 质量控制模块接口适配器

   4   *

   5   * 将现有质量控制服务适配为标准接口，实现模块独立性。

   6   */

   7  

   8  const db = require('../config/database');

   9  const logger = require('../config/logger');

  10  const { safeExecuteWithRetry, createModuleError } = require('../middleware/module-error-handler');

  11  const moduleRegistry = require('../config/module-registry');

  12  

  13  class QualityControlModuleAdapter {

  14    constructor() {

  15      this.name = 'quality-control';

  16      this.version = '1.0.0';

  17      this.config = null;

  18      this.initialized = false;

  19    }

  20  

  21    /**

  22     * 模块初始化

  23     *

  24     * @param {Object} config - 模块配置

  25     */

  26    async initialize(config = {}) {

  27      this.config = {

  28        warningDays: 30,

  29        autoRemind: true,

  30        maxPageSize: 2000,

  31        defaultPageSize: 20,

  32        ...config,

  33      };

  34      this.initialized = true;

  35      logger.info('质量控制模块初始化完成');

  36    }

  37  

  38    /**

  39     * 根据ID查找质控记录

  40     *

  41     * @param {number} id - 记录ID

  42     * @param {number} tenantId - 租户ID

  43     * @returns {Promise<Object|null>}

  44     */

  45    async findById(id, tenantId) {

  46      const [rows] = await db.execute(

  47        'SELECT * FROM quality_control_records WHERE id = ? AND tenant_id = ? AND is_deleted = 0',

  48        [id, tenantId],

  49      );

  50      return rows.length > 0 ? rows[0] : null;


*第 7 页*

---

## 第 8 页

   1    }

   2  

   3    /**

   4     * 查询质控记录列表

   5     *

   6     * @param {Object} filters - 过滤条件

   7     * @param {number} tenantId - 租户ID

   8     * @param {Object} pagination - 分页参数

   9     * @returns {Promise<{data: Array, total: number}>}

  10     */

  11    async findAll(filters, tenantId, pagination = {}) {

  12      const { page = 1, pageSize = this.config.defaultPageSize } = pagination;

  13      const offset = (page - 1) * pageSize;

  14  

  15      let whereClause = 'WHERE tenant_id = ? AND is_deleted = 0';

  16      const params = [tenantId];

  17  

  18      if (filters.status) {

  19        whereClause += ' AND status = ?';

  20        params.push(filters.status);

  21      }

  22      if (filters.result) {

  23        whereClause += ' AND result = ?';

  24        params.push(filters.result);

  25      }

  26      if (filters.qcType) {

  27        whereClause += ' AND qc_type = ?';

  28        params.push(filters.qcType);

  29      }

  30      if (filters.assetCode) {

  31        whereClause += ' AND asset_code LIKE ?';

  32        params.push(`%${filters.assetCode}%`);

  33      }

  34      if (filters.startDate) {

  35        whereClause += ' AND qc_date >= ?';

  36        params.push(filters.startDate);

  37      }

  38      if (filters.endDate) {

  39        whereClause += ' AND qc_date <= ?';

  40        params.push(filters.endDate);

  41      }

  42  

  43      const [countResult] = await db.execute(

  44        `SELECT COUNT(*) as total FROM quality_control_records ${whereClause}`,

  45        params,

  46      );

  47  

  48      const [rows] = await db.execute(

  49        `SELECT * FROM quality_control_records ${whereClause} ORDER BY qc_date DESC LIMIT ? OFFSET ?`,

  50        [...params, pageSize, offset],


*第 8 页*

---

## 第 9 页

   1      );

   2  

   3      return {

   4        data: rows,

   5        total: countResult[0].total,

   6        page,

   7        pageSize,

   8      };

   9    }

  10  

  11    /**

  12     * 创建质控记录

  13     *

  14     * @param {Object} data - 质控数据

  15     * @param {number} tenantId - 租户ID

  16     * @param {string} createdBy - 创建人

  17     * @returns {Promise<Object>}

  18     */

  19    async create(data, tenantId, createdBy) {

  20      const { record_no, asset_code, asset_name, qc_type, qc_date, qc_item, qc_method } = data;

  21  

  22      // 自动生成下次质控日期

  23      let nextQcDate = data.next_qc_date;

  24      if (!nextQcDate && data.qc_cycle) {

  25        nextQcDate = new Date(qc_date);

  26        nextQcDate.setMonth(nextQcDate.getMonth() + data.qc_cycle);

  27      }

  28  

  29      const [result] = await db.execute(

  30        `INSERT INTO quality_control_records 

  31         (tenant_id, record_no, asset_code, asset_name, qc_type, qc_date, next_qc_date, 

  32          qc_item, qc_method, qc_standard, qc_person, department, created_by, created_at)

  33         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, NOW())`,

  34        [

  35          tenantId, record_no, asset_code, asset_name, qc_type, qc_date, nextQcDate,

  36          qc_item, qc_method, data.qc_standard, data.qc_person, data.department, createdBy,

  37        ],

  38      );

  39  

  40      const record = await this.findById(result.insertId, tenantId);

  41  

  42      // 触发事件

  43      moduleRegistry.emitEvent('quality-control', 'created', {

  44        id: result.insertId,

  45        recordNo: record_no,

  46        assetCode: asset_code,

  47        tenantId,

  48        createdBy,

  49      });

  50  


*第 9 页*

---

## 第 10 页

   1      return record;

   2    }

   3  

   4    /**

   5     * 更新质控记录

   6     *

   7     * @param {number} id - 记录ID

   8     * @param {Object} data - 更新数据

   9     * @param {number} tenantId - 租户ID

  10     * @param {string} updatedBy - 更新人

  11     * @returns {Promise<Object>}

  12     */

  13    async update(id, data, tenantId, updatedBy) {

  14      const existing = await this.findById(id, tenantId);

  15      if (!existing) {

  16        throw createModuleError('NOT_FOUND', `质控记录 ${id} 不存在`);

  17      }

  18  

  19      const updates = [];

  20      const values = [];

  21      const allowedFields = [

  22        'qc_type', 'qc_date', 'next_qc_date', 'qc_item', 'qc_method',

  23        'qc_standard', 'qc_value', 'standard_value', 'deviation', 'tolerance',

  24        'result', 'qc_person', 'department', 'status', 'remark',

  25        'reviewer', 'review_date', 'qc_cycle', 'warning_days', 'is_auto_remind',

  26      ];

  27  

  28      for (const [key, value] of Object.entries(data)) {

  29        if (allowedFields.includes(key) && value !== undefined) {

  30          updates.push(`${key} = ?`);

  31          values.push(value);

  32        }

  33      }

  34  

  35      if (updates.length === 0) {

  36        return existing;

  37      }

  38  

  39      updates.push('updated_at = NOW()');

  40      updates.push('updated_by = ?');

  41      values.push(updatedBy);

  42      values.push(id);

  43      values.push(tenantId);

  44  

  45      await db.execute(

  46        `UPDATE quality_control_records SET ${updates.join(', ')} WHERE id = ? AND tenant_id = ?`,

  47        values,

  48      );

  49  

  50      const record = await this.findById(id, tenantId);


*第 10 页*

---

## 第 11 页

   1  

   2      // 触发事件

   3      moduleRegistry.emitEvent('quality-control', 'updated', {

   4        id,

   5        tenantId,

   6        updatedBy,

   7        changes: data,

   8      });

   9  

  10      return record;

  11    }

  12  

  13    /**

  14     * 删除质控记录（软删除）

  15     *

  16     * @param {number} id - 记录ID

  17     * @param {number} tenantId - 租户ID

  18     * @returns {Promise<boolean>}

  19     */

  20    async delete(id, tenantId) {

  21      const existing = await this.findById(id, tenantId);

  22      if (!existing) {

  23        throw createModuleError('NOT_FOUND', `质控记录 ${id} 不存在`);

  24      }

  25  

  26      await db.execute(

  27        'UPDATE quality_control_records SET is_deleted = 1 WHERE id = ? AND tenant_id = ?',

  28        [id, tenantId],

  29      );

  30  

  31      // 触发事件

  32      moduleRegistry.emitEvent('quality-control', 'deleted', {

  33        id,

  34        tenantId,

  35        recordNo: existing.record_no,

  36      });

  37  

  38      return true;

  39    }

  40  

  41    /**

  42     * 统计质控记录数量

  43     *

  44     * @param {Object} filters - 过滤条件

  45     * @param {number} tenantId - 租户ID

  46     * @returns {Promise<number>}

  47     */

  48    async count(filters, tenantId) {

  49      let whereClause = 'WHERE tenant_id = ? AND is_deleted = 0';

  50      const params = [tenantId];


*第 11 页*

---

## 第 12 页

   1  

   2      if (filters.status) {

   3        whereClause += ' AND status = ?';

   4        params.push(filters.status);

   5      }

   6      if (filters.result) {

   7        whereClause += ' AND result = ?';

   8        params.push(filters.result);

   9      }

  10  

  11      const [result] = await db.execute(

  12        `SELECT COUNT(*) as total FROM quality_control_records ${whereClause}`,

  13        params,

  14      );

  15  

  16      return result[0].total;

  17    }

  18  

  19    /**

  20     * 获取统计信息

  21     *

  22     * @param {number} tenantId - 租户ID

  23     * @returns {Promise<Object>}

  24     */

  25    async getStatistics(tenantId) {

  26      const [result] = await db.execute(

  27        `SELECT 

  28          COUNT(*) as total,

  29          COUNT(CASE WHEN status = '已完成' THEN 1 END) as completed,

  30          COUNT(CASE WHEN status = '待检' THEN 1 END) as pending,

  31          COUNT(CASE WHEN result = '合格' THEN 1 END) as passed,

  32          COUNT(CASE WHEN result = '不合格' THEN 1 END) as failed,

  33          COUNT(CASE WHEN next_qc_date <= DATE_ADD(NOW(), INTERVAL ? DAY) AND next_qc_date >= NOW() THEN 1 END) as upcoming,

  34          COUNT(CASE WHEN next_qc_date < NOW() AND status != '已完成' THEN 1 END) as overdue

  35         FROM quality_control_records

  36         WHERE tenant_id = ? AND is_deleted = 0`,

  37        [this.config.warningDays, tenantId],

  38      );

  39  

  40      return result[0];

  41    }

  42  

  43    /**

  44     * 模块启动

  45     */

  46    async start() {

  47      logger.info('质量控制模块已启动');

  48    }

  49  

  50    /**


*第 12 页*

---

## 第 13 页

   1     * 模块停止

   2     */

   3    async stop() {

   4      logger.info('质量控制模块已停止');

   5    }

   6  

   7    /**

   8     * 模块销毁

   9     */

  10    async destroy() {

  11      this.initialized = false;

  12      logger.info('质量控制模块已销毁');

  13    }

  14  }

  15  

  16  module.exports = QualityControlModuleAdapter;

  17  

  18  

  19  // ===== FILE: backend/architecture/module-interfaces.js =====

  20  /**

  21   * 模块化架构规范定义

  22   *

  23   * 本文件定义了 AssetHost 系统的模块化架构标准，

  24   * 确保每个模块能够独立运行、维护和扩展。

  25   */

  26  

  27  // ============================================

  28  // 1. 标准模块接口定义

  29  // ============================================

  30  

  31  /**

  32   * 标准服务接口

  33   *

  34   * 每个业务服务模块应实现以下接口：

  35   *

  36   * @interface

  37   * @property {Function} findById - 根据ID查找记录

  38   * @property {Function} findAll - 查询记录列表（支持分页和过滤）

  39   * @property {Function} create - 创建新记录

  40   * @property {Function} update - 更新现有记录

  41   * @property {Function} delete - 删除记录（软删除）

  42   * @property {Function} count - 统计记录数量

  43   */

  44  const StandardServiceInterface = {

  45    /**

  46     * @param {number} id - 记录ID

  47     * @param {number} tenantId - 租户ID

  48     * @returns {Promise<Object|null>} 记录对象或null

  49     */

  50    async findById(id, tenantId) {},


*第 13 页*

---

## 第 14 页

   1  

   2    /**

   3     * @param {Object} filters - 过滤条件

   4     * @param {number} tenantId - 租户ID

   5     * @param {Object} pagination - 分页参数 {page, pageSize}

   6     * @returns {Promise<{data: Array, total: number}>}

   7     */

   8    async findAll(filters, tenantId, pagination) {},

   9  

  10    /**

  11     * @param {Object} data - 记录数据

  12     * @param {number} tenantId - 租户ID

  13     * @param {string} createdBy - 创建人

  14     * @returns {Promise<Object>} 创建的记录

  15     */

  16    async create(data, tenantId, createdBy) {},

  17  

  18    /**

  19     * @param {number} id - 记录ID

  20     * @param {Object} data - 更新数据

  21     * @param {number} tenantId - 租户ID

  22     * @param {string} updatedBy - 更新人

  23     * @returns {Promise<Object>} 更新后的记录

  24     */

  25    async update(id, data, tenantId, updatedBy) {},

  26  

  27    /**

  28     * @param {number} id - 记录ID

  29     * @param {number} tenantId - 租户ID

  30     * @returns {Promise<boolean>} 是否删除成功

  31     */

  32    async delete(id, tenantId) {},

  33  

  34    /**

  35     * @param {Object} filters - 过滤条件

  36     * @param {number} tenantId - 租户ID

  37     * @returns {Promise<number>} 记录数量

  38     */

  39    async count(filters, tenantId) {},

  40  };

  41  

  42  // ============================================

  43  // 2. 模块配置接口定义

  44  // ============================================

  45  

  46  /**

  47   * 标准模块配置接口

  48   *

  49   * 每个模块应提供以下配置项：

  50   *


*第 14 页*

---

## 第 15 页

   1   * @interface

   2   * @property {string} name - 模块名称

   3   * @property {string} version - 模块版本

   4   * @property {boolean} enabled - 是否启用

   5   * @property {Object} defaults - 默认配置

   6   * @property {Array<string>} dependencies - 依赖的其他模块

   7   * @property {Object} validation - 配置验证规则

   8   */

   9  const ModuleConfigInterface = {

  10    name: '',

  11    version: '1.0.0',

  12    enabled: true,

  13    defaults: {},

  14    dependencies: [],

  15    validation: {},

  16  };

  17  

  18  // ============================================

  19  // 3. 事件接口定义

  20  // ============================================

  21  

  22  /**

  23   * 标准模块事件接口

  24   *

  25   * 模块间通过事件进行松耦合通信：

  26   *

  27   * @interface

  28   * @property {string} MODULE_NAME - 模块名称前缀

  29   * @property {Function} emit - 触发事件

  30   * @property {Function} on - 监听事件

  31   * @property {Function} off - 移除监听

  32   */

  33  const ModuleEventInterface = {

  34    MODULE_NAME: 'module',

  35  

  36    /**

  37     * 触发模块事件

  38     * @param {string} eventName - 事件名称（不含前缀）

  39     * @param {Object} payload - 事件数据

  40     */

  41    emit(eventName, payload) {},

  42  

  43    /**

  44     * 监听模块事件

  45     * @param {string} eventName - 事件名称（不含前缀）

  46     * @param {Function} handler - 事件处理函数

  47     */

  48    on(eventName, handler) {},

  49  

  50    /**


*第 15 页*

---

## 第 16 页

   1     * 移除事件监听

   2     * @param {string} eventName - 事件名称（不含前缀）

   3     * @param {Function} handler - 事件处理函数

   4     */

   5    off(eventName, handler) {},

   6  };

   7  

   8  // ============================================

   9  // 4. 错误处理接口定义

  10  // ============================================

  11  

  12  /**

  13   * 标准模块错误类型

  14   */

  15  const ModuleError = {

  16    /** 参数错误 */

  17    VALIDATION_ERROR: 'VALIDATION_ERROR',

  18    /** 记录不存在 */

  19    NOT_FOUND: 'NOT_FOUND',

  20    /** 权限不足 */

  21    FORBIDDEN: 'FORBIDDEN',

  22    /** 业务规则冲突 */

  23    CONFLICT: 'CONFLICT',

  24    /** 外部依赖失败 */

  25    DEPENDENCY_ERROR: 'DEPENDENCY_ERROR',

  26    /** 系统内部错误 */

  27    INTERNAL_ERROR: 'INTERNAL_ERROR',

  28  };

  29  

  30  /**

  31   * 标准模块异常类

  32   */

  33  class ModuleException extends Error {

  34    /**

  35     * @param {string} code - 错误代码

  36     * @param {string} message - 错误消息

  37     * @param {Object} details - 详细信息

  38     * @param {Error} cause - 原始异常

  39     */

  40    constructor(code, message, details = {}, cause = null) {

  41      super(message);

  42      this.name = 'ModuleException';

  43      this.code = code;

  44      this.details = details;

  45      this.cause = cause;

  46      this.timestamp = new Date().toISOString();

  47    }

  48  

  49    /** 转换为API响应格式 */

  50    toResponse() {


*第 16 页*

---

## 第 17 页

   1      return {

   2        success: false,

   3        code: this.code,

   4        message: this.message,

   5        details: this.details,

   6        timestamp: this.timestamp,

   7      };

   8    }

   9  }

  10  

  11  // ============================================

  12  // 5. 模块生命周期接口

  13  // ============================================

  14  

  15  /**

  16   * 模块生命周期钩子

  17   *

  18   * @interface

  19   * @property {Function} initialize - 模块初始化

  20   * @property {Function} validate - 配置验证

  21   * @property {Function} start - 模块启动

  22   * @property {Function} stop - 模块停止

  23   * @property {Function} destroy - 模块销毁

  24   */

  25  const ModuleLifecycleInterface = {

  26    /**

  27     * 初始化模块（加载配置、建立连接等）

  28     * @param {Object} config - 模块配置

  29     * @returns {Promise<void>}

  30     */

  31    async initialize(config) {},

  32  

  33    /**

  34     * 验证模块配置是否有效

  35     * @param {Object} config - 模块配置

  36     * @returns {Promise<boolean>} 是否有效

  37     */

  38    async validate(config) {},

  39  

  40    /**

  41     * 启动模块（开始处理请求、启动定时任务等）

  42     * @returns {Promise<void>}

  43     */

  44    async start() {},

  45  

  46    /**

  47     * 停止模块（停止处理请求、清理资源等）

  48     * @returns {Promise<void>}

  49     */

  50    async stop() {},


*第 17 页*

---

## 第 18 页

   1  

   2    /**

   3     * 销毁模块（释放所有资源）

   4     * @returns {Promise<void>}

   5     */

   6    async destroy() {},

   7  };

   8  

   9  // ============================================

  10  // 6. 数据访问接口定义

  11  // ============================================

  12  

  13  /**

  14   * 标准数据访问接口（Repository模式）

  15   *

  16   * @interface

  17   * @property {Function} findById - 根据ID查询

  18   * @property {Function} findWhere - 条件查询

  19   * @property {Function} insert - 插入记录

  20   * @property {Function} updateById - 根据ID更新

  21   * @property {Function} deleteById - 根据ID删除

  22   * @property {Function} transaction - 执行事务

  23   */

  24  const RepositoryInterface = {

  25    /**

  26     * @param {number} id - 记录ID

  27     * @param {number} tenantId - 租户ID

  28     * @returns {Promise<Object|null>}

  29     */

  30    async findById(id, tenantId) {},

  31  

  32    /**

  33     * @param {Object} where - 查询条件

  34     * @param {number} tenantId - 租户ID

  35     * @param {Object} options - 查询选项 {orderBy, limit, offset}

  36     * @returns {Promise<Array>}

  37     */

  38    async findWhere(where, tenantId, options = {}) {},

  39  

  40    /**

  41     * @param {Object} data - 记录数据

  42     * @param {number} tenantId - 租户ID

  43     * @returns {Promise<Object>}

  44     */

  45    async insert(data, tenantId) {},

  46  

  47    /**

  48     * @param {number} id - 记录ID

  49     * @param {Object} data - 更新数据

  50     * @param {number} tenantId - 租户ID


*第 18 页*

---

## 第 19 页

   1     * @returns {Promise<boolean>}

   2     */

   3    async updateById(id, data, tenantId) {},

   4  

   5    /**

   6     * @param {number} id - 记录ID

   7     * @param {number} tenantId - 租户ID

   8     * @returns {Promise<boolean>}

   9     */

  10    async deleteById(id, tenantId) {},

  11  

  12    /**

  13     * @param {Function} callback - 事务回调

  14     * @returns {Promise<any>}

  15     */

  16    async transaction(callback) {},

  17  };

  18  

  19  // ============================================

  20  // 7. 导出

  21  // ============================================

  22  

  23  module.exports = {

  24    StandardServiceInterface,

  25    ModuleConfigInterface,

  26    ModuleEventInterface,

  27    ModuleLifecycleInterface,

  28    RepositoryInterface,

  29    ModuleError,

  30    ModuleException,

  31  };

  32  

  33  

  34  // ===== FILE: backend/calculate-token-count.js =====

  35  const fs = require('fs');

  36  

  37  /**

  38   * 计算token数量的简单估算函数

  39   * 基于OpenAI的估算：1个token ≈ 4个字符

  40   * @param {string} text - 文本内容

  41   * @returns {number} 估算的token数量

  42   */

  43  function estimateTokenCount(text) {

  44    if (!text) return 0;

  45    // 简单估算：1 token ≈ 4 characters

  46    return Math.ceil(text.length / 4);

  47  }

  48  

  49  /**

  50   * 模拟构建AI分析提示词的过程


*第 19 页*

---

## 第 20 页

   1   */

   2  async function calculateAIPromptTokenCount() {

   3    try {

   4      console.log('=== 计算AI分析提示词Token数量 ===\n');

   5  

   6      // 从MD文件读取数据库结构

   7      const mdFilePath =

   8        '/Users/cjlee/Desktop/Mac_Win_share/AssetHost/资产管理vite/zcgl-database-structure.md';

   9      let dbStructureContent;

  10      try {

  11        dbStructureContent = fs.readFileSync(mdFilePath, 'utf8');

  12        console.log(`成功读取数据库结构文件: ${mdFilePath}`);

  13      } catch (error) {

  14        console.log(`数据库结构文件不存在，使用默认结构描述: ${error.message}`);

  15        // 使用默认的数据库结构描述

  16        dbStructureContent = `# 资产管理系统数据库结构

  17  

  18  ## 核心表结构

  19  

  20  ### 资产主表（assets）

  21  - id: 资产ID，主键

  22  - asset_code: 资产编号

  23  - asset_name: 资产名称

  24  - brand: 品牌

  25  - model: 型号

  26  - specification: 规格

  27  - department: 所属科室

  28  - department_new: 新所属科室

  29  - status: 资产状态

  30  - purchase_date: 购买日期

  31  - purchase_price: 购买价格

  32  

  33  ### 科室表（departments）

  34  - id: 科室ID，主键

  35  - department_code: 科室编码

  36  - department_name: 科室名称

  37  - tenant_id: 租户ID

  38  

  39  ### 资产分类表（asset_categories）

  40  - id: 分类ID，主键

  41  - category_code: 分类编码

  42  - category_name: 分类名称

  43  

  44  ### 资产变更日志表（asset_change_logs）

  45  - id: 日志ID，主键

  46  - asset_id: 资产ID

  47  - change_type: 变更类型

  48  - change_content: 变更内容

  49  - created_at: 创建时间`;

  50      }


*第 20 页*

---

## 第 21 页

   1  

   2      console.log(`1. 原始数据库结构文件大小: ${dbStructureContent.length} 字符`);

   3      console.log(`   估算token数: ${estimateTokenCount(dbStructureContent)}`);

   4  

   5      // 只保留与资产分析相关的核心表结构

   6      const coreTables = [

   7        '资产主表（assets）',

   8        '科室表（departments）',

   9        '资产分类表（asset_categories）',

  10        '资产变更日志表（asset_change_logs）',

  11      ];

  12  

  13      // 解析并过滤数据库结构，只保留核心表

  14      const lines = dbStructureContent.split('\n');

  15      const filteredLines = [];

  16      let includeLine = false;

  17  

  18      for (const line of lines) {

  19        // 检查是否是表定义行

  20        const tableMatch = line.match(/^表\d+：(.*?)$/);

  21        if (tableMatch) {

  22          const tableName = tableMatch[1];

  23          includeLine = coreTables.some(coreTable => tableName.includes(coreTable));

  24        }

  25  

  26        // 如果包含当前行或者是标题行，添加到过滤后的内容中

  27        if (includeLine || line.startsWith('#') || line.startsWith('数据库名：')) {

  28          filteredLines.push(line);

  29        }

  30      }

  31  

  32      // 重新构建过滤后的数据库结构内容

  33      dbStructureContent = filteredLines.join('\n');

  34  

  35      console.log(`\n2. 过滤后核心表结构大小: ${dbStructureContent.length} 字符`);

  36      console.log(`   估算token数: ${estimateTokenCount(dbStructureContent)}`);

  37  

  38      // 构建约束条件

  39      const constraints = `

  40  ## 指令约束

  41  0.不可以生成增加、删除和更新的数据库语句

  42  1. 严格根据上述数据库结构生成SQL，禁止使用未定义的表/字段/关联关系；

  43  2. 生成的SQL为标准[MySQL]语法，适配对应数据库版本，无需额外注释；

  44  3. 处理时间/数值字段时保留原字段类型，无需类型转换，条件判断使用精准运算符；

  45  4. 若用户需求涉及多表关联，必须使用显式JOIN（INNER JOIN/LEFT JOIN），禁止隐式连接；

  46  5. 若用户需求模糊（如未指定时间范围），仅生成基础SQL框架，不随意补充条件；

  47  6. 直接输出SQL语句，无其他多余文字（如"以下是生成的SQL"）；

  48  7. 当需要查询科室资产时，科室的匹配使用LIKE而不使用=。

  49  

  50  ### 示例查询


*第 21 页*

---

## 第 22 页

   1  1.查询某资产明细，以病理科为例：SELECT a.*

   2   FROM assets a

   3   INNER JOIN departments d ON a.department = d.department_name

   4   WHERE d.department_name LIKE '%病理科%';

   5  

   6  2.查询资产总明细，以显微镜为例：SELECT 

   7       a.asset_name,

   8       a.asset_code,

   9       a.brand,

  10       a.model,

  11       a.department,

  12       a.use_department,

  13       a.location,

  14       a.status,

  15       a.purchase_date,

  16       a.purchase_price,

  17       a.current_value,

  18       a.depreciation_method,

  19       a.depreciation_years,

  20       a.warranty_period,

  21       a.warranty_end_date,

  22       a.created_at

  23   FROM assets a

  24   WHERE a.asset_name LIKE '%显微镜%'

  25      `.trim();

  26  

  27      console.log(`\n3. 指令约束和示例查询大小: ${constraints.length} 字符`);

  28      console.log(`   估算token数: ${estimateTokenCount(constraints)}`);

  29  

  30      // 用户问题

  31      const userPrompt = '病理科资产情况';

  32  

  33      console.log(`\n4. 用户问题大小: ${userPrompt.length} 字符`);

  34      console.log(`   估算token数: ${estimateTokenCount(userPrompt)}`);

  35  

  36      // 构建完整提示词

  37      const fullPrompt = `

  38  ## 数据库结构信息

  39  ${dbStructureContent}

  40  

  41  ${constraints}

  42  

  43  ## 用户问题

  44  ${userPrompt}

  45      `.trim();

  46  

  47      console.log(`\n5. 完整提示词大小: ${fullPrompt.length} 字符`);

  48      console.log(`   估算token数: ${estimateTokenCount(fullPrompt)}`);

  49  

  50      // 计算总token数


*第 22 页*

---

## 第 23 页

   1      const totalTokens = estimateTokenCount(fullPrompt);

   2  

   3      console.log('\n=== 最终估算结果 ===');

   4      console.log(`发送"病理科资产情况"分析请求需要上传的token数: ${totalTokens}`);

   5      console.log('\n注意: 这是基于字符数的估算值，实际token数可能会有所不同。');

   6      console.log('不同AI模型的token计算方式可能略有差异。');

   7    } catch (error) {

   8      console.error('计算token数量失败:', error.message);

   9    }

  10  }

  11  

  12  // 运行计算

  13  calculateAIPromptTokenCount();

  14  

  15  

  16  // ===== FILE: backend/check_module_tables.js =====

  17  const db = require('./config/database');

  18  

  19  async function checkTables() {

  20    try {

  21      const result = await db.execute('SHOW TABLES LIKE "%module%"');

  22      console.log('模块相关表:');

  23      result[0].forEach(t => {

  24        console.log('  -', Object.values(t)[0]);

  25      });

  26    } catch (error) {

  27      console.error('Error:', error.message);

  28    }

  29    process.exit(0);

  30  }

  31  

  32  checkTables();

  33  

  34  

  35  // ===== FILE: backend/check_modules_db.js =====

  36  const mysql = require('mysql2/promise');

  37  

  38  async function checkModulesDatabase() {

  39    console.log('🔍 检查远程数据库中的系统模块表...');

  40  

  41    // 数据库连接配置

  42    const dbConfig = {

  43      host: '101.37.236.101',

  44      port: 3306,

  45      user: 'root',

  46      password: "***",

  47      database: 'zcgl',

  48    };

  49  

  50    let connection;


*第 23 页*

---

## 第 24 页

   1  

   2    try {

   3      // 连接数据库

   4      console.log('📡 正在连接数据库...');

   5      connection = await mysql.createConnection(dbConfig);

   6      console.log('✅ 数据库连接成功!');

   7  

   8      // 检查是否存在system_modules表

   9      console.log('\n📋 检查system_modules表是否存在:');

  10      const [tables] = await connection.execute('SHOW TABLES LIKE "system_modules"');

  11  

  12      if (tables.length === 0) {

  13        console.error('❌ system_modules表不存在!');

  14  

  15        // 检查是否有其他system_开头的表

  16        console.log('\n🔍 检查其他system_开头的表:');

  17        const [systemTables] = await connection.execute('SHOW TABLES LIKE "system%"');

  18  

  19        if (systemTables.length === 0) {

  20          console.log('⚠️  没有找到任何system_开头的表');

  21        } else {

  22          console.log('✅ 找到的system_开头的表:');

  23          systemTables.forEach((table, index) => {

  24            console.log(`${index + 1}. ${Object.values(table)[0]}`);

  25          });

  26        }

  27      } else {

  28        console.log('✅ system_modules表存在!');

  29  

  30        // 查看表结构

  31        console.log('\n📊 查看system_modules表结构:');

  32        const [structure] = await connection.execute('DESCRIBE system_modules');

  33        structure.forEach(field => {

  34          console.log(

  35            `${field.Field} (${field.Type}) - ${field.Null === 'YES' ? 'NULL' : 'NOT NULL'} - ${field.Default || '无默认值'} - ${field.Key}`,

  36          );

  37        });

  38  

  39        // 查看表数据

  40        console.log('\n📈 查看system_modules表数据:');

  41        const [data] = await connection.execute('SELECT * FROM system_modules');

  42  

  43        if (data.length === 0) {

  44          console.warn('⚠️  system_modules表为空!');

  45        } else {

  46          console.log(`✅ 找到 ${data.length} 条记录:`);

  47          data.forEach((module, index) => {

  48            console.log(`\n${index + 1}. 模块ID: ${module.id}`);

  49            console.log(`   模块名称: ${module.module_name}`);

  50            console.log(`   模块代码: ${module.module_code}`);


*第 24 页*

---

## 第 25 页

   1            console.log(`   模块路径: ${module.module_path}`);

   2            console.log(`   排序: ${module.sort_order}`);

   3            console.log(`   状态: ${module.status}`);

   4          });

   5        }

   6      }

   7  

   8      // 检查是否存在system_module_roles表

   9      console.log('\n📋 检查system_module_roles表是否存在:');

  10      const [rolesTables] = await connection.execute('SHOW TABLES LIKE "system_module_roles"');

  11  

  12      if (rolesTables.length === 0) {

  13        console.warn('⚠️  system_module_roles表不存在!');

  14      } else {

  15        console.log('✅ system_module_roles表存在!');

  16  

  17        // 查看表数据

  18        const [rolesData] = await connection.execute('SELECT * FROM system_module_roles');

  19        console.log(`📈 system_module_roles表有 ${rolesData.length} 条记录`);

  20      }

  21  

  22      // 检查是否存在system_module_menus表

  23      console.log('\n📋 检查system_module_menus表是否存在:');

  24      const [menusTables] = await connection.execute('SHOW TABLES LIKE "system_module_menus"');

  25  

  26      if (menusTables.length === 0) {

  27        console.warn('⚠️  system_module_menus表不存在!');

  28      } else {

  29        console.log('✅ system_module_menus表存在!');

  30  

  31        // 查看表数据

  32        const [menusData] = await connection.execute('SELECT * FROM system_module_menus');

  33        console.log(`📈 system_module_menus表有 ${menusData.length} 条记录`);

  34      }

  35    } catch (error) {

  36      console.error('❌ 数据库操作失败:', error.message);

  37    } finally {

  38      if (connection) {

  39        await connection.end();

  40        console.log('\n🔌 数据库连接已关闭');

  41      }

  42    }

  43  }

  44  

  45  // 运行检查

  46  checkModulesDatabase();

  47  

  48  

  49  // ===== FILE: backend/check_remote_tables.js =====

  50  const mysql = require('mysql2/promise');


*第 25 页*

---

## 第 26 页

   1  

   2  // 远程数据库配置

   3  const config = {

   4    host: '101.37.236.101',

   5    port: 3306,

   6    user: 'root',

   7    password: "***",

   8    database: 'zcgl',

   9  };

  10  

  11  async function checkRemoteTables() {

  12    let connection;

  13  

  14    try {

  15      // 连接到远程数据库

  16      connection = await mysql.createConnection(config);

  17      console.log('✅ 成功连接到远程数据库');

  18  

  19      // 获取所有表名

  20      console.log('\n📋 获取所有表名...');

  21      const [tables] = await connection.execute(`

  22        SHOW TABLES

  23      `);

  24  

  25      console.log('现有表名:');

  26      tables.forEach((table, index) => {

  27        const tableName = table[`Tables_in_${config.database}`];

  28        console.log(`  ${index + 1}. ${tableName}`);

  29      });

  30  

  31      // 查找包含部门相关的表

  32      const departmentLikeTables = tables.filter(table => {

  33        const tableName = table[`Tables_in_${config.database}`].toLowerCase();

  34        return tableName.includes('dept') || tableName.includes('department');

  35      });

  36  

  37      if (departmentLikeTables.length > 0) {

  38        console.log('\n🔍 发现部门相关表:');

  39        for (const table of departmentLikeTables) {

  40          const tableName = table[`Tables_in_${config.database}`];

  41          console.log(`\n📊 ${tableName} 表结构:`);

  42          const [columns] = await connection.execute(`DESCRIBE ${tableName}`);

  43          console.table(columns);

  44  

  45          console.log(`📋 ${tableName} 示例数据:`);

  46          const [data] = await connection.execute(`SELECT * FROM ${tableName} LIMIT 5`);

  47          console.table(data);

  48        }

  49      } else {

  50        console.log('\n❌ 未发现部门相关表');


*第 26 页*

---

## 第 27 页

   1      }

   2  

   3      // 检查assets表结构以确认新字段已添加

   4      console.log('\n🔍 assets表结构:');

   5      const [assetsColumns] = await connection.execute('DESCRIBE assets');

   6      console.table(assetsColumns);

   7    } catch (error) {

   8      console.error('❌ 操作失败:', error.message);

   9    } finally {

  10      if (connection) {

  11        await connection.end();

  12        console.log('\n✅ 数据库连接已关闭');

  13      }

  14    }

  15  }

  16  

  17  // 执行检查

  18  checkRemoteTables();

  19  

  20  

  21  // ===== FILE: backend/check_workorder_consistency.js =====

  22  const db = require('./config/database');

  23  

  24  async function checkWorkOrderConsistency() {

  25    console.log('='.repeat(60));

  26    console.log('AssetHost 工单与成本数据一致性检查');

  27    console.log('='.repeat(60));

  28  

  29    const results = {

  30      workOrders: { total: 0, tableOk: false },

  31      workOrderHistory: { total: 0, orphaned: 0 },

  32      maintenanceRequests: { total: 0 },

  33      maintenanceCosts: { total: 0, orphaned: 0 },

  34      workOrderMaterialCosts: { total: 0, orphaned: 0 },

  35      statusTransitions: [],

  36      relationshipChecks: [],

  37    };

  38  

  39    try {

  40      // 1. 检查 work_orders 表结构和数据

  41      console.log('\n[1] 检查 work_orders 表...');

  42  

  43      const [woTable] = await db.execute('DESCRIBE work_orders');

  44      console.log(`   表结构字段数: ${woTable.length}`);

  45      const requiredFields = ['id', 'work_order_no', 'tenant_id', 'asset_code', 'status', 'title', 'source_type', 'created_at'];

  46      const missingFields = requiredFields.filter(f => !woTable.find(t => t.Field === f));

  47      if (missingFields.length > 0) {

  48        console.log(`   ❌ 缺少字段: ${missingFields.join(', ')}`);

  49      } else {

  50        console.log('   ✓ 所有必要字段存在');


*第 27 页*

---

## 第 28 页

   1      }

   2  

   3      const [woCount] = await db.execute('SELECT COUNT(*) as cnt FROM work_orders');

   4      results.workOrders.total = woCount[0].cnt;

   5      console.log(`   总工单数: ${results.workOrders.total}`);

   6      results.workOrders.tableOk = true;

   7  

   8      // 2. 检查状态分布

   9      console.log('\n[2] 检查工单状态分布...');

  10      const [statusDist] = await db.execute(`

  11        SELECT status, COUNT(*) as cnt 

  12        FROM work_orders 

  13        GROUP BY status 

  14        ORDER BY cnt DESC

  15      `);

  16      statusDist.forEach(s => console.log(`   ${s.status}: ${s.cnt}个`));

  17      results.statusTransitions = statusDist;

  18  

  19      // 3. 检查 work_order_history 表

  20      console.log('\n[3] 检查 work_order_history 表...');

  21      const [histTable] = await db.execute('DESCRIBE work_order_history');

  22      console.log(`   表结构字段数: ${histTable.length}`);

  23  

  24      const [histCount] = await db.execute('SELECT COUNT(*) as cnt FROM work_order_history');

  25      results.workOrderHistory.total = histCount[0].cnt;

  26      console.log(`   总历史记录数: ${results.workOrderHistory.total}`);

  27  

  28      // 检查孤立的history记录（work_order_id不存在）

  29      const [orphanedHist] = await db.execute(`

  30        SELECT COUNT(*) as cnt 

  31        FROM work_order_history h 

  32        LEFT JOIN work_orders w ON h.work_order_id = w.id 

  33        WHERE w.id IS NULL

  34      `);

  35      results.workOrderHistory.orphaned = orphanedHist[0].cnt;

  36      if (orphanedHist[0].cnt > 0) {

  37        console.log(`   ⚠️ 孤立历史记录: ${orphanedHist[0].cnt}条`);

  38      } else {

  39        console.log('   ✓ 无孤立历史记录');

  40      }

  41  

  42      // 检查没有历史记录的工单

  43      const [noHistWo] = await db.execute(`

  44        SELECT COUNT(*) as cnt 

  45        FROM work_orders w 

  46        LEFT JOIN work_order_history h ON w.id = h.work_order_id 

  47        WHERE h.id IS NULL

  48      `);

  49      console.log(`   无历史记录的工单: ${noHistWo[0].cnt}个`);

  50  


*第 28 页*

---

## 第 29 页

   1      // 4. 检查 maintenance_requests 表

   2      console.log('\n[4] 检查 maintenance_requests 表...');

   3      const [reqCount] = await db.execute('SELECT COUNT(*) as cnt FROM maintenance_requests');

   4      results.maintenanceRequests.total = reqCount[0].cnt;

   5      console.log(`   总维修申请数: ${results.maintenanceRequests.total}`);

   6  

   7      // 状态分布

   8      const [reqStatusDist] = await db.execute(`

   9        SELECT status, COUNT(*) as cnt 

  10        FROM maintenance_requests 

  11        GROUP BY status 

  12        ORDER BY cnt DESC

  13      `);

  14      reqStatusDist.forEach(s => console.log(`   ${s.status}: ${s.cnt}个`));

  15  

  16      // 5. 检查 maintenance_costs 表

  17      console.log('\n[5] 检查 maintenance_costs 表...');

  18      const [costCount] = await db.execute('SELECT COUNT(*) as cnt FROM maintenance_costs');

  19      results.maintenanceCosts.total = costCount[0].cnt;

  20      console.log(`   总成本记录数: ${results.maintenanceCosts.total}`);

  21  

  22      // 检查关联字段

  23      const [costWithLog] = await db.execute(`

  24        SELECT COUNT(*) as cnt FROM maintenance_costs WHERE maintenance_log_id IS NOT NULL

  25      `);

  26      const [costWithWo] = await db.execute(`

  27        SELECT COUNT(*) as cnt FROM maintenance_costs WHERE work_order_id IS NOT NULL

  28      `);

  29      console.log(`   关联维修日志的记录: ${costWithLog[0].cnt}`);

  30      console.log(`   关联工单的记录: ${costWithWo[0].cnt}`);

  31  

  32      // 6. 检查 maintenance_workorders（旧表）

  33      console.log('\n[6] 检查 maintenance_workorders 表（旧工单表）...');

  34      const [legacyCount] = await db.execute('SELECT COUNT(*) as cnt FROM maintenance_workorders');

  35      console.log(`   旧工单数: ${legacyCount[0].cnt}`);

  36  

  37      const [legacyStatusDist] = await db.execute(`

  38        SELECT status, COUNT(*) as cnt 

  39        FROM maintenance_workorders 

  40        GROUP BY status 

  41        ORDER BY cnt DESC

  42      `);

  43      legacyStatusDist.forEach(s => console.log(`   ${s.status}: ${s.cnt}个`));

  44  

  45      // 7. 检查工单与维修申请的关联

  46      console.log('\n[7] 检查工单与维修申请关联...');

  47  

  48      // 从维修申请创建的工单

  49      const [woFromRequests] = await db.execute(`

  50        SELECT COUNT(*) as cnt 


*第 29 页*

---

## 第 30 页

   1        FROM work_orders 

   2        WHERE source_type = 'repair_request'

   3      `);

   4      console.log(`   来源为维修申请的工单: ${woFromRequests[0].cnt}`);

   5  

   6      // 8. 检查成本数据与维修日志关联

   7      console.log('\n[8] 检查成本数据与维修日志关联...');

   8      const [logCount] = await db.execute('SELECT COUNT(*) as cnt FROM maintenance_logs');

   9      console.log(`   维修日志总数: ${logCount[0].cnt}`);

  10  

  11      // 成本趋势数据（从维修日志计算）

  12      const [costTrendFromLogs] = await db.execute(`

  13        SELECT COUNT(*) as cnt, SUM(maintenance_cost) as total 

  14        FROM maintenance_logs

  15      `);

  16      console.log(`   维修日志成本汇总: ${costTrendFromLogs[0].cnt}条记录, 总金额 ${costTrendFromLogs[0].total || 0}`);

  17  

  18      // 9. 检查数据完整性问题

  19      console.log('\n[9] 检查数据完整性问题...');

  20  

  21      // 检查没有关联资产的工单

  22      const [woNoAsset] = await db.execute(`

  23        SELECT COUNT(*) as cnt 

  24        FROM work_orders w 

  25        LEFT JOIN assets a ON w.asset_code = a.asset_code AND w.tenant_id = a.tenant_id 

  26        WHERE a.id IS NULL

  27      `);

  28      console.log(`   无效资产关联的工单: ${woNoAsset[0].cnt}`);

  29  

  30      // 检查缺失关键字段的工单

  31      const [woNoTitle] = await db.execute(`

  32        SELECT COUNT(*) as cnt FROM work_orders WHERE title IS NULL OR title = ''

  33      `);

  34      console.log(`   缺失标题的工单: ${woNoTitle[0].cnt}`);

  35  

  36      // 检查缺失负责人的工单（但状态不是pending）

  37      const [woNoAssignNoPending] = await db.execute(`

  38        SELECT COUNT(*) as cnt 

  39        FROM work_orders 

  40        WHERE (assigned_to IS NULL OR assigned_to = '') 

  41        AND status != 'pending'

  42      `);

  43      console.log(`   非待处理但无负责人的工单: ${woNoAssignNoPending[0].cnt}`);

  44  

  45      // 10. 检查状态流转逻辑

  46      console.log('\n[10] 检查状态流转逻辑...');

  47  

  48      // 检查不应存在的状态组合

  49      const [invalidStatus1] = await db.execute(`

  50        SELECT COUNT(*) as cnt 


*第 30 页*

---

## 第 31 页

   1      }

   2    }

   3  

   4    trace(...args) {

   5      if (this.enabled) {

   6        console.trace('[TRACE]', ...args);

   7      }

   8    }

   9  

  10    assert(condition, ...args) {

  11      if (!condition) {

  12        this.error('Assertion failed:', ...args);

  13      }

  14    }

  15  

  16    clear() {

  17      if (this.enabled) {

  18        console.clear();

  19      }

  20    }

  21  

  22    dir(obj) {

  23      if (this.enabled) {

  24        console.dir(obj);

  25      }

  26    }

  27  

  28    dirxml(obj) {

  29      if (this.enabled) {

  30        console.dirxml(obj);

  31      }

  32    }

  33  }

  34  

  35  class SimpleErrorReporter {

  36    constructor() {

  37      this.errors = [];

  38      this.warnings = [];

  39      this.maxErrors = 100;

  40      this.maxWarnings = 50;

  41    }

  42  

  43    error(...args) {

  44      const errorEntry = {

  45        timestamp: new Date().toISOString(),

  46        level: 'error',

  47        message: args.map(arg => 

  48          typeof arg === 'object' ? JSON.stringify(arg) : String(arg)

  49        ).join(' '),

  50        stack: new Error().stack,


*第 31 页*

---

## 第 32 页

   1        url: window.location.href,

   2        userAgent: navigator.userAgent,

   3      };

   4  

   5      this.errors.push(errorEntry);

   6  

   7      // 保持错误数量在限制内

   8      if (this.errors.length > this.maxErrors) {

   9        this.errors.shift();

  10      }

  11  

  12      // 可以在这里发送到服务器

  13      this.sendToServer(errorEntry);

  14    }

  15  

  16    warn(...args) {

  17      const warningEntry = {

  18        timestamp: new Date().toISOString(),

  19        level: 'warning',

  20        message: args.map(arg => 

  21          typeof arg === 'object' ? JSON.stringify(arg) : String(arg)

  22        ).join(' '),

  23        url: window.location.href,

  24      };

  25  

  26      this.warnings.push(warningEntry);

  27  

  28      if (this.warnings.length > this.maxWarnings) {

  29        this.warnings.shift();

  30      }

  31  

  32      this.sendToServer(warningEntry);

  33    }

  34  

  35    sendToServer(entry) {

  36      // 生产环境发送到错误收集服务

  37      if (isProduction) {

  38        try {

  39          // 这里可以集成到 Sentry、Bugsnag 等服务

  40          // 或者发送到自己的错误收集 API

  41          // fetch('/api/client-errors', {

  42          //   method: 'POST',

  43          //   headers: { 'Content-Type': 'application/json' },

  44          //   body: JSON.stringify(entry),

  45          //   keepalive: true,

  46          // });

  47        } catch (e) {

  48          // 避免错误上报本身导致更多错误

  49        }

  50      }


*第 32 页*

---

## 第 33 页

   1    }

   2  

   3    getErrors() {

   4      return this.errors;

   5    }

   6  

   7    getWarnings() {

   8      return this.warnings;

   9    }

  10  

  11    clearErrors() {

  12      this.errors = [];

  13    }

  14  

  15    clearWarnings() {

  16      this.warnings = [];

  17    }

  18  

  19    getReport() {

  20      return {

  21        errors: this.errors,

  22        warnings: this.warnings,

  23        summary: {

  24          totalErrors: this.errors.length,

  25          totalWarnings: this.warnings.length,

  26          firstError: this.errors[0]?.timestamp,

  27          lastError: this.errors[this.errors.length - 1]?.timestamp,

  28        },

  29      };

  30    }

  31  }

  32  

  33  // 创建全局单例

  34  const productionLogger = new ProductionLogger();

  35  const errorReporter = new SimpleErrorReporter();

  36  

  37  // 设置错误上报器

  38  productionLogger.setErrorReporter(errorReporter);

  39  

  40  // 导出

  41  export const logger = productionLogger;

  42  export const reporter = errorReporter;

  43  

  44  // 设置全局错误处理器

  45  if (isProduction) {

  46    window.onerror = (message, source, lineno, colno, error) => {

  47      productionLogger.error('全局错误:', {

  48        message,

  49        source,

  50        lineno,


*第 33 页*

---

## 第 34 页

   1        colno,

   2        stack: error?.stack,

   3      });

   4    };

   5  

   6    window.onunhandledrejection = (event) => {

   7      productionLogger.error('未处理的Promise拒绝:', {

   8        reason: event.reason,

   9      });

  10    };

  11  }

  12  

  13  // Vue/React 组件错误边界（React示例）

  14  export const withErrorBoundary = (Component, fallback) => {

  15    return class ErrorBoundary extends React.Component {

  16      constructor(props) {

  17        super(props);

  18        this.state = { hasError: false };

  19      }

  20  

  21      static getDerivedStateFromError(error) {

  22        productionLogger.error('组件渲染错误:', error);

  23        return { hasError: true };

  24      }

  25  

  26      componentDidCatch(error, errorInfo) {

  27        productionLogger.error('组件错误:', error, errorInfo);

  28      }

  29  

  30      render() {

  31        if (this.state.hasError) {

  32          return fallback || <div>组件加载失败</div>;

  33        }

  34        return <Component {...this.props} />;

  35      }

  36    };

  37  };

  38  

  39  export default productionLogger;

  40  

  41  

  42  // ===== FILE: frontend/src/utils/request-batcher.js =====

  43  /**

  44   * API 请求合并工具

  45   * 将多个相似的请求合并为一个批量请求，减少服务器压力

  46   */

  47  

  48  import { api } from '../api/client';

  49  

  50  class RequestBatcher {


*第 34 页*

---

## 第 35 页

   1    constructor(options = {}) {

   2      this.batchWindow = options.batchWindow || 50; // 合并窗口期（毫秒）

   3      this.maxBatchSize = options.maxBatchSize || 50; // 最大批量大小

   4      this.batches = new Map(); // 待处理的批次

   5      this.timeouts = new Map(); // 批次超时

   6    }

   7  

   8    /**

   9     * 生成批次键

  10     */

  11    generateBatchKey(method, url) {

  12      return `${method}:${url}`;

  13    }

  14  

  15    /**

  16     * 添加到批次

  17     */

  18    addToBatch(method, url, data, resolve, reject) {

  19      const key = this.generateBatchKey(method, url);

  20  

  21      if (!this.batches.has(key)) {

  22        this.batches.set(key, {

  23          method,

  24          url,

  25          items: [],

  26          promises: [],

  27        });

  28      }

  29  

  30      const batch = this.batches.get(key);

  31      batch.items.push(data);

  32      batch.promises.push({ resolve, reject });

  33  

  34      // 如果达到最大批次大小，立即执行

  35      if (batch.items.length >= this.maxBatchSize) {

  36        this.executeBatch(key);

  37      } else {

  38        // 否则设置延迟执行

  39        this.scheduleBatch(key);

  40      }

  41    }

  42  

  43    /**

  44     * 设置批次执行计划

  45     */

  46    scheduleBatch(key) {

  47      // 清除现有的超时

  48      if (this.timeouts.has(key)) {

  49        clearTimeout(this.timeouts.get(key));

  50      }


*第 35 页*

---

## 第 36 页

   1  

   2      // 设置新的超时

   3      const timeout = setTimeout(() => {

   4        this.executeBatch(key);

   5      }, this.batchWindow);

   6  

   7      this.timeouts.set(key, timeout);

   8    }

   9  

  10    /**

  11     * 执行批次请求

  12     */

  13    async executeBatch(key) {

  14      const batch = this.batches.get(key);

  15      if (!batch || batch.items.length === 0) return;

  16  

  17      // 从待处理中移除

  18      this.batches.delete(key);

  19      this.timeouts.delete(key);

  20  

  21      try {

  22        // 发送批量请求

  23        const response = await api.post(batch.url, {

  24          batch: true,

  25          items: batch.items,

  26        });

  27  

  28        // 分发响应到各个 Promise

  29        if (response && response.results) {

  30          batch.promises.forEach((promise, index) => {

  31            const result = response.results[index];

  32            if (result && result.success !== false) {

  33              promise.resolve(result);

  34            } else {

  35              promise.reject(new Error(result?.message || 'Batch request failed'));

  36            }

  37          });

  38        } else {

  39          // 如果没有单独的结果，全部使用同一响应

  40          batch.promises.forEach(promise => {

  41            promise.resolve(response);

  42          });

  43        }

  44      } catch (error) {

  45        // 所有 Promise 都失败

  46        batch.promises.forEach(promise => {

  47          promise.reject(error);

  48        });

  49      }

  50    }


*第 36 页*

---

## 第 37 页

   1  

   2    /**

   3     * 批量请求方法

   4     */

   5    async batchRequest(method, url, data) {

   6      return new Promise((resolve, reject) => {

   7        this.addToBatch(method, url, data, resolve, reject);

   8      });

   9    }

  10  

  11    /**

  12     * 清除所有待处理的批次

  13     */

  14    clear() {

  15      for (const timeout of this.timeouts.values()) {

  16        clearTimeout(timeout);

  17      }

  18      this.batches.clear();

  19      this.timeouts.clear();

  20    }

  21  }

  22  

  23  // 创建全局实例

  24  const requestBatcher = new RequestBatcher();

  25  

  26  export default requestBatcher;

  27  

  28  /**

  29   * 自动批量请求 Hook

  30   * 用于 React 组件中自动合并请求

  31   */

  32  export const useBatchedRequest = () => {

  33    const batchRequest = async (url, data) => {

  34      return requestBatcher.batchRequest('POST', url, data);

  35    };

  36  

  37    return { batchRequest };

  38  };

  39  

  40  /**

  41   * 批量操作工具

  42   * 用于批量更新、删除等操作

  43   */

  44  export class BatchOperation {

  45    constructor(options = {}) {

  46      this.items = [];

  47      this.batchSize = options.batchSize || 100;

  48      this.onProgress = options.onProgress || (() => {});

  49      this.onComplete = options.onComplete || (() => {});

  50      this.onError = options.onError || (() => {});


*第 37 页*

---

## 第 38 页

   1    }

   2  

   3    /**

   4     * 添加操作项

   5     */

   6    add(item) {

   7      this.items.push(item);

   8    }

   9  

  10    /**

  11     * 执行批量操作

  12     */

  13    async execute(asyncFn) {

  14      const results = [];

  15      const errors = [];

  16      const total = this.items.length;

  17  

  18      for (let i = 0; i < total; i += this.batchSize) {

  19        const batch = this.items.slice(i, i + this.batchSize);

  20  

  21        try {

  22          const batchResults = await Promise.all(

  23            batch.map(item => asyncFn(item).catch(error => ({ error, item })))

  24          );

  25  

  26          batchResults.forEach((result, index) => {

  27            if (result && result.error) {

  28              errors.push({ item: batch[index], error: result.error });

  29            } else {

  30              results.push(result);

  31            }

  32          });

  33  

  34          this.onProgress({

  35            completed: Math.min(i + this.batchSize, total),

  36            total,

  37            percentage: Math.round((Math.min(i + this.batchSize, total) / total) * 100),

  38          });

  39        } catch (error) {

  40          this.onError(error);

  41          errors.push(...batch.map(item => ({ item, error })));

  42        }

  43      }

  44  

  45      this.onComplete({ results, errors });

  46      return { results, errors };

  47    }

  48  }

  49  

  50  /**


*第 38 页*

---

## 第 39 页

   1   * 使用示例：

   2   * 

   3   * // 1. 请求缓存

   4   * import requestCache, { CACHE_CONFIG } from '@/utils/request-cache';

   5   * 

   6   * // 带缓存的 GET 请求

   7   * const users = await requestCache.get('/api/users', {

   8   *   cache: true,

   9   *   cacheTTL: CACHE_CONFIG.MEDIUM_TERM

  10   * });

  11   * 

  12   * // 2. 批量请求

  13   * import requestBatcher from '@/utils/request-batcher';

  14   * 

  15   * const result = await requestBatcher.batchRequest('POST', '/api/assets/batch-update', {

  16   *   id: assetId,

  17   *   data: updateData

  18   * });

  19   * 

  20   * // 3. 批量操作

  21   * import { BatchOperation } from '@/utils/request-batcher';

  22   * 

  23   * const batch = new BatchOperation({

  24   *   batchSize: 50,

  25   *   onProgress: ({ percentage }) => console.log(`${percentage}% 完成`),

  26   * });

  27   * 

  28   * assets.forEach(asset => batch.add(asset));

  29   * const { results, errors } = await batch.execute(async (asset) => {

  30   *   return api.put(`/api/assets/${asset.id}`, asset);

  31   * });

  32   */

  33  

  34  

  35  // ===== FILE: frontend/src/utils/request-cache.js =====

  36  /**

  37   * API 请求缓存工具

  38   * 提供请求去重、响应缓存、失效策略等功能

  39   */

  40  

  41  import { api } from '../api/client';

  42  

  43  class RequestCache {

  44    constructor() {

  45      this.cache = new Map();

  46      this.pendingRequests = new Map();

  47      this.defaultTTL = 5 * 60 * 1000; // 默认缓存5分钟

  48    }

  49  

  50    /**


*第 39 页*

---

## 第 40 页

   1     * 生成缓存键

   2     */

   3    generateKey(method, url, params = {}) {

   4      const sortedParams = Object.keys(params)

   5        .sort()

   6        .map(key => `${key}=${JSON.stringify(params[key])}`)

   7        .join('&');

   8      return `${method}:${url}?${sortedParams}`;

   9    }

  10  

  11    /**

  12     * 获取缓存数据

  13     */

  14    getCached(key) {

  15      const item = this.cache.get(key);

  16      if (!item) return null;

  17  

  18      // 检查是否过期

  19      if (Date.now() > item.expiry) {

  20        this.cache.delete(key);

  21        return null;

  22      }

  23  

  24      return item.data;

  25    }

  26  

  27    /**

  28     * 设置缓存数据

  29     */

  30    set(key, data, ttl = this.defaultTTL) {

  31      this.cache.set(key, {

  32        data,

  33        expiry: Date.now() + ttl,

  34      });

  35    }

  36  

  37    /**

  38     * 删除缓存

  39     */

  40    deleteCached(key) {

  41      this.cache.delete(key);

  42    }

  43  

  44    /**

  45     * 清除所有缓存

  46     */

  47    clear() {

  48      this.cache.clear();

  49    }

  50  


*第 40 页*

---

## 第 41 页

   1    /**

   2     * 清除匹配的缓存

   3     */

   4    clearMatching(pattern) {

   5      for (const key of this.cache.keys()) {

   6        if (key.includes(pattern)) {

   7          this.cache.delete(key);

   8        }

   9      }

  10    }

  11  

  12    /**

  13     * 带缓存的请求

  14     */

  15    async request(method, url, options = {}) {

  16      const { 

  17        cache = false, 

  18        cacheTTL = this.defaultTTL,

  19        cacheKey: customCacheKey,

  20        ...requestOptions 

  21      } = options;

  22  

  23      const cacheKey = customCacheKey || this.generateKey(method, url, requestOptions.params);

  24  

  25      // 如果启用缓存，先检查缓存

  26      if (cache) {

  27        const cached = this.getCached(cacheKey);

  28        if (cached) {

  29          return cached;

  30        }

  31      }

  32  

  33      // 检查是否有正在进行的相同请求（请求去重）

  34      if (this.pendingRequests.has(cacheKey)) {

  35        return this.pendingRequests.get(cacheKey);

  36      }

  37  

  38      // 发起请求

  39      const requestPromise = api.request({

  40        method,

  41        url,

  42        ...requestOptions,

  43      }).then(response => {

  44        // 缓存响应

  45        if (cache && response) {

  46          this.set(cacheKey, response, cacheTTL);

  47        }

  48        // 移除待处理请求

  49        this.pendingRequests.delete(cacheKey);

  50        return response;


*第 41 页*

---

## 第 42 页

   1      }).catch(error => {

   2        // 移除待处理请求

   3        this.pendingRequests.delete(cacheKey);

   4        throw error;

   5      });

   6  

   7      // 记录待处理请求

   8      this.pendingRequests.set(cacheKey, requestPromise);

   9  

  10      return requestPromise;

  11    }

  12  

  13    /**

  14     * GET 请求（带缓存）

  15     */

  16    async get(url, options = {}) {

  17      return this.request('get', url, options);

  18    }

  19  

  20    /**

  21     * POST 请求（默认不缓存）

  22     */

  23    async post(url, options = {}) {

  24      return this.request('post', url, { ...options, cache: false });

  25    }

  26  

  27    /**

  28     * PUT 请求（清除相关缓存）

  29     */

  30    async put(url, options = {}) {

  31      // 清除相关缓存

  32      this.clearMatching(url);

  33      return this.request('put', url, { ...options, cache: false });

  34    }

  35  

  36    /**

  37     * DELETE 请求（清除相关缓存）

  38     */

  39    async delete(url, options = {}) {

  40      // 清除相关缓存

  41      this.clearMatching(url);

  42      return this.request('delete', url, { ...options, cache: false });

  43    }

  44  

  45    /**

  46     * 获取缓存统计

  47     */

  48    getStats() {

  49      let valid = 0;

  50      let expired = 0;


*第 42 页*

---

## 第 43 页

   1  

   2      for (const [, item] of this.cache.entries()) {

   3        if (Date.now() > item.expiry) {

   4          expired++;

   5        } else {

   6          valid++;

   7        }

   8      }

   9  

  10      return {

  11        total: this.cache.size,

  12        valid,

  13        expired,

  14        pendingRequests: this.pendingRequests.size,

  15      };

  16    }

  17  

  18    /**

  19     * 清理过期缓存

  20     */

  21    cleanup() {

  22      const now = Date.now();

  23      for (const [key, item] of this.cache.entries()) {

  24        if (now > item.expiry) {

  25          this.cache.delete(key);

  26        }

  27      }

  28    }

  29  }

  30  

  31  // 创建全局实例

  32  const requestCache = new RequestCache();

  33  

  34  // 定期清理过期缓存（每5分钟）

  35  const cacheCleanupInterval = setInterval(() => {

  36    requestCache.cleanup();

  37  }, 5 * 60 * 1000);

  38  

  39  // 提供清理函数，用于应用卸载时调用

  40  export const cleanupRequestCache = () => {

  41    clearInterval(cacheCleanupInterval);

  42    requestCache.clear();

  43  };

  44  

  45  export default requestCache;

  46  

  47  // 缓存配置常量

  48  export const CACHE_CONFIG = {

  49    // 长时间缓存（1小时）

  50    LONG_TERM: 60 * 60 * 1000,


*第 43 页*

---

## 第 44 页

   1  

   2    // 中等时间缓存（15分钟）

   3    MEDIUM_TERM: 15 * 60 * 1000,

   4  

   5    // 短时间缓存（5分钟）

   6    SHORT_TERM: 5 * 60 * 1000,

   7  

   8    // 实时数据（不缓存）

   9    REAL_TIME: 0,

  10  };

  11  

  12  // 常用 API 缓存配置

  13  export const API_CACHE_STRATEGIES = {

  14    // 用户列表 - 短时间缓存

  15    'GET:/api/users': { cache: true, ttl: CACHE_CONFIG.SHORT_TERM },

  16  

  17    // 资产分类 - 长时间缓存

  18    'GET:/api/assets/categories': { cache: true, ttl: CACHE_CONFIG.LONG_TERM },

  19  

  20    // 部门列表 - 中等时间缓存

  21    'GET:/api/departments': { cache: true, ttl: CACHE_CONFIG.MEDIUM_TERM },

  22  

  23    // 系统配置 - 长时间缓存

  24    'GET:/api/system-config': { cache: true, ttl: CACHE_CONFIG.LONG_TERM },

  25  

  26    // 模块配置 - 长时间缓存

  27    'GET:/api/module-configs': { cache: true, ttl: CACHE_CONFIG.LONG_TERM },

  28  

  29    // 仪表盘统计 - 短时间缓存

  30    'GET:/api/dashboard': { cache: true, ttl: CACHE_CONFIG.SHORT_TERM },

  31  

  32    // 资产详情 - 中等时间缓存

  33    'GET:/api/assets/*': { cache: true, ttl: CACHE_CONFIG.MEDIUM_TERM },

  34  };

  35  

  36  

  37  // ===== FILE: frontend/src/utils/roleUtils.js =====

  38  /**

  39   * 角色相关工具函数

  40   */

  41  import crypto from './crypto';

  42  

  43  // ==================== 角色定义 ====================

  44  

  45  // 管理员角色列表

  46  export const ADMIN_ROLES = ['super_admin', 'system_admin'];

  47  

  48  // 业务管理员角色列表（拥有比普通用户更多的权限）

  49  export const BUSINESS_ADMIN_ROLES = [

  50    'asset_admin',


*第 44 页*

---

## 第 45 页

   1    'department_admin',

   2    'metrology_admin',

   3    'quality_admin',

   4    'maintenance_admin',

   5    'acceptance_admin',

   6    'transfer_admin',

   7    'inventory_admin',

   8  ];

   9  

  10  // 所有系统角色列表

  11  export const ALL_SYSTEM_ROLES = [...ADMIN_ROLES, ...BUSINESS_ADMIN_ROLES, 'user'];

  12  

  13  // 角色显示名称映射

  14  export const ROLE_DISPLAY_NAMES = {

  15    super_admin: '超级管理员',

  16    system_admin: '系统管理员',

  17    asset_admin: '资产管理员',

  18    department_admin: '科室管理员',

  19    metrology_admin: '计量管理员',

  20    quality_admin: '质量管理员',

  21    maintenance_admin: '维护管理员',

  22    acceptance_admin: '验收管理员',

  23    transfer_admin: '调配管理员',

  24    inventory_admin: '盘点管理员',

  25    user: '普通用户',

  26  };

  27  

  28  // 角色权限级别（数字越大权限越高）

  29  export const ROLE_LEVELS = {

  30    super_admin: 100,

  31    system_admin: 90,

  32    asset_admin: 70,

  33    department_admin: 60,

  34    metrology_admin: 65,

  35    quality_admin: 65,

  36    maintenance_admin: 65,

  37    acceptance_admin: 65,

  38    transfer_admin: 65,

  39    inventory_admin: 65,

  40    user: 10,

  41  };

  42  

  43  // ==================== 角色检查函数 ====================

  44  

  45  /**

  46   * 检查是否为管理员角色

  47   * @param {string} role - 用户角色

  48   * @returns {boolean} 是否为管理员

  49   */

  50  export const isAdminRole = role => ADMIN_ROLES.includes(role);


*第 45 页*

---

## 第 46 页

   1  

   2  export const isSuperAdmin = user => {

   3    if (!user) return false;

   4    return user.is_super_admin === true || user.role === 'super_admin';

   5  };

   6  

   7  export const isBusinessAdminRole = role => BUSINESS_ADMIN_ROLES.includes(role);

   8  

   9  /**

  10   * 检查是否为系统角色

  11   * @param {string} role - 用户角色

  12   * @returns {boolean} 是否为系统角色

  13   */

  14  export const isSystemRole = role => ALL_SYSTEM_ROLES.includes(role);

  15  

  16  /**

  17   * 获取角色显示名称

  18   * @param {string} role - 角色代码

  19   * @returns {string} 显示名称

  20   */

  21  export const getRoleDisplayName = role => ROLE_DISPLAY_NAMES[role] || role || '未知角色';

  22  

  23  /**

  24   * 获取角色权限级别

  25   * @param {string} role - 角色代码

  26   * @returns {number} 权限级别

  27   */

  28  export const getRoleLevel = role => ROLE_LEVELS[role] || 0;

  29  

  30  /**

  31   * 比较两个角色的权限级别

  32   * @param {string} roleA - 角色A

  33   * @param {string} roleB - 角色B

  34   * @returns {number} 正数表示A权限更高，负数表示B权限更高，0表示相等

  35   */

  36  export const compareRoleLevel = (roleA, roleB) => getRoleLevel(roleA) - getRoleLevel(roleB);

  37  

  38  /**

  39   * 检查角色A是否拥有比角色B更高的或相等的权限

  40   * @param {string} roleA - 角色A

  41   * @param {string} roleB - 角色B

  42   * @returns {boolean}

  43   */

  44  export const hasHigherOrEqualRole = (roleA, roleB) => compareRoleLevel(roleA, roleB) >= 0;

  45  

  46  // ==================== 菜单权限检查 ====================

  47  

  48  /**

  49   * 超级管理员专属菜单key列表

  50   */


*第 46 页*

---

## 第 47 页

   1  export const SUPER_ADMIN_ONLY_MENUS = ['/database-connection', '/api-docs', '/api-documentation'];

   2  

   3  /**

   4   * 检查是否为超级管理员专属菜单

   5   * @param {string} menuKey - 菜单路径

   6   * @returns {boolean} 是否为超级管理员专属

   7   */

   8  export const isSuperAdminOnlyMenu = menuKey => SUPER_ADMIN_ONLY_MENUS.includes(menuKey);

   9  

  10  /**

  11   * 检查用户是否有访问指定菜单的权限

  12   * @param {Object} user - 用户对象

  13   * @param {string} menuKey - 菜单路径

  14   * @param {string[]} visibleMenuKeys - 用户可见的菜单键列表

  15   * @param {boolean} menuPermissionLoaded - 权限是否已加载

  16   * @returns {boolean} 是否有权限

  17   */

  18  export const canAccessMenu = (user, menuKey, visibleMenuKeys = [], menuPermissionLoaded = false) => {

  19    // 超级管理员总是可以访问所有菜单

  20    if (user?.role === 'super_admin') return true;

  21  

  22    // 超级管理员专属菜单检查

  23    if (isSuperAdminOnlyMenu(menuKey) && user?.role !== 'super_admin') {

  24      return false;

  25    }

  26  

  27    // 管理员总是可以访问非超级管理员专属菜单

  28    if (isAdminRole(user?.role)) return true;

  29  

  30    // 权限未加载时默认允许（避免页面闪烁）

  31    if (!menuPermissionLoaded) return true;

  32  

  33    // 可见菜单为空时允许（兼容旧版本）

  34    if (!visibleMenuKeys || visibleMenuKeys.length === 0) return true;

  35  

  36    // 检查是否在可见菜单中

  37    return visibleMenuKeys.includes(menuKey);

  38  };

  39  

  40  /**

  41   * 检查用户是否可以访问某个功能模块

  42   * @param {Object} user - 用户对象

  43   * @param {string} moduleId - 模块ID

  44   * @returns {boolean}

  45   */

  46  export const canAccessModule = (user, moduleId) => {

  47    if (!user || !moduleId) return false;

  48  

  49    // 超级管理员可以访问所有模块

  50    if (user.role === 'super_admin') return true;


*第 47 页*

---

## 第 48 页

   1  

   2    // 检查用户启用的模块列表

   3    const enabledModules = user.enabled_modules || [];

   4    if (enabledModules.includes(moduleId)) return true;

   5  

   6    // 某些模块有依赖关系

   7    const moduleFallbacks = {

   8      'special-equipment-management': ['compliance-management'],

   9      'safety-inspection-management': ['compliance-management'],

  10      'asset-risk-management': ['compliance-management'],

  11      'staff-qualification': ['compliance-management'],

  12      'uptime-management': ['compliance-management'],

  13      'iot-geo-location-management': ['iot-management'],

  14      'iot-zone-location-management': ['iot-management'],

  15      'iot-asset-monitoring-management': ['iot-management'],

  16      'iot-environment-monitoring-management': ['iot-management'],

  17      'iot-patient-volume-management': ['iot-management'],

  18    };

  19  

  20    const fallbacks = moduleFallbacks[moduleId] || [];

  21    return fallbacks.some(fallbackId => enabledModules.includes(fallbackId));

  22  };

  23  

  24  // ==================== 用户相关函数 ====================

  25  

  26  /**

  27   * 从localStorage获取当前用户

  28   * @returns {Object|null} 用户对象

  29   */

  30  export const getCurrentUser = () => {

  31    return crypto.getItem('user');

  32  };

  33  

  34  /**

  35   * 检查当前用户是否为管理员

  36   * @returns {boolean} 是否为管理员

  37   */

  38  export const isCurrentUserAdmin = () => {

  39    const user = getCurrentUser();

  40    return isAdminRole(user?.role);

  41  };

  42  

  43  /**

  44   * 检查当前用户是否为超级管理员

  45   * @returns {boolean} 是否为超级管理员

  46   */

  47  export const isCurrentUserSuperAdmin = () => {

  48    const user = getCurrentUser();

  49    return user?.role === 'super_admin';

  50  };


*第 48 页*

---

## 第 49 页

   1  

   2  /**

   3   * 获取当前用户的角色显示名称

   4   * @returns {string}

   5   */

   6  export const getCurrentUserRoleDisplayName = () => {

   7    const user = getCurrentUser();

   8    return getRoleDisplayName(user?.role);

   9  };

  10  

  11  /**

  12   * 检查当前用户是否拥有指定角色或更高级别

  13   * @param {string} requiredRole - 要求的最低角色

  14   * @returns {boolean}

  15   */

  16  export const currentUserHasRole = requiredRole => {

  17    const user = getCurrentUser();

  18    if (!user?.role) return false;

  19    return hasHigherOrEqualRole(user.role, requiredRole);

  20  };

  21  

  22  

  23  // ===== FILE: frontend/src/utils/safeConsole.js =====

  24  /**

  25   * API 响应处理工具

  26   * 提供统一的错误处理和用户提示

  27   */

  28  

  29  import { message } from 'antd';

  30  import { logger } from './productionLogger';

  31  

  32  class APIResponseHandler {

  33    constructor() {

  34      this.errorCount = new Map();

  35      this.maxErrorsPerEndpoint = 5;

  36      this.errorTimeWindow = 60000; // 1分钟内

  37    }

  38  

  39    /**

  40     * 检查是否应该显示错误提示

  41     */

  42    shouldShowError(endpoint) {

  43      const now = Date.now();

  44      const endpointErrors = this.errorCount.get(endpoint) || [];

  45  

  46      // 清理过期记录

  47      const recentErrors = endpointErrors.filter(

  48        time => now - time < this.errorTimeWindow

  49      );

  50  


*第 49 页*

---

## 第 50 页

   1      this.errorCount.set(endpoint, recentErrors);

   2  

   3      // 如果1分钟内错误次数超过阈值，不再显示提示

   4      if (recentErrors.length >= this.maxErrorsPerEndpoint) {

   5        return false;

   6      }

   7  

   8      // 记录错误

   9      recentErrors.push(now);

  10      return true;

  11    }

  12  

  13    /**

  14     * 处理成功响应

  15     */

  16    handleSuccess(result, options = {}) {

  17      const {

  18        showMessage = true,

  19        successMessage = '操作成功',

  20        silentEndpoints = [],

  21      } = options;

  22  

  23      if (result.success && showMessage && result.message) {

  24        message.success(result.message);

  25      }

  26  

  27      logger.info('API请求成功', {

  28        url: result.url,

  29        success: result.success,

  30      });

  31  

  32      return result;

  33    }

  34  

  35    /**

  36     * 处理错误响应

  37     */

  38    handleError(error, options = {}) {

  39      const {

  40        endpoint = 'unknown',

  41        showMessage = true,

  42        customMessage = null,

  43        silentEndpoints = [],

  44        fallbackMessage = '操作失败，请稍后重试',

  45      } = options;

  46  

  47      // 检查是否应该显示错误

  48      const shouldShow = this.shouldShowError(endpoint);

  49  

  50      // 记录错误


*第 50 页*

---

## 第 51 页

   1      logger.error('API请求错误', {

   2        endpoint,

   3        error: error.message || error,

   4        status: error.response?.status,

   5        data: error.response?.data,

   6      });

   7  

   8      // 显示用户提示

   9      if (showMessage && shouldShow) {

  10        let displayMessage = customMessage || 

  11          error.response?.data?.message || 

  12          error.message || 

  13          fallbackMessage;

  14  

  15        // 根据错误类型选择提示方式

  16        if (error.response?.status === 401) {

  17          message.error('登录已过期，请重新登录');

  18        } else if (error.response?.status === 403) {

  19          message.error('没有权限执行此操作');

  20        } else if (error.response?.status === 404) {

  21          message.error('请求的资源不存在');

  22        } else if (error.response?.status >= 500) {

  23          message.error('服务器错误，请联系管理员');

  24        } else {

  25          message.error(displayMessage);

  26        }

  27      }

  28  

  29      // 返回标准化的错误对象

  30      return {

  31        success: false,

  32        error: error.message || 'Unknown error',

  33        code: error.response?.data?.code || error.code || 'UNKNOWN_ERROR',

  34        status: error.response?.status,

  35        data: error.response?.data,

  36        timestamp: new Date().toISOString(),

  37      };

  38    }

  39  

  40    /**

  41     * 处理网络错误

  42     */

  43    handleNetworkError(error, options = {}) {

  44      const { showMessage = true } = options;

  45  

  46      logger.error('网络错误', {

  47        error: error.message || error,

  48        type: 'NETWORK_ERROR',

  49      });

  50  


*第 51 页*

---

## 第 52 页

   1      if (showMessage) {

   2        if (!navigator.onLine) {

   3          message.error('网络连接已断开，请检查网络');

   4        } else {

   5          message.error('网络请求失败，请稍后重试');

   6        }

   7      }

   8  

   9      return {

  10        success: false,

  11        error: error.message || 'Network error',

  12        code: 'NETWORK_ERROR',

  13        timestamp: new Date().toISOString(),

  14      };

  15    }

  16  

  17    /**

  18     * 处理超时错误

  19     */

  20    handleTimeoutError(options = {}) {

  21      const { showMessage = true } = options;

  22  

  23      logger.warn('请求超时');

  24  

  25      if (showMessage) {

  26        message.warning('请求超时，请稍后重试');

  27      }

  28  

  29      return {

  30        success: false,

  31        error: 'Request timeout',

  32        code: 'TIMEOUT_ERROR',

  33        timestamp: new Date().toISOString(),

  34      };

  35    }

  36  

  37    /**

  38     * 重试处理

  39     */

  40    async withRetry(fn, options = {}) {

  41      const {

  42        maxRetries = 3,

  43        retryDelay = 1000,

  44        onRetry = null,

  45        shouldRetry = (error) => {

  46          // 默认只重试网络错误和服务器错误

  47          return !error.response || error.response.status >= 500;

  48        },

  49      } = options;

  50  


*第 52 页*

---

## 第 53 页

   1      let lastError;

   2  

   3      for (let i = 0; i < maxRetries; i++) {

   4        try {

   5          return await fn();

   6        } catch (error) {

   7          lastError = error;

   8  

   9          if (i < maxRetries - 1 && shouldRetry(error)) {

  10            logger.info(`重试请求 (${i + 1}/${maxRetries})`);

  11  

  12            if (onRetry) {

  13              onRetry(error, i + 1);

  14            }

  15  

  16            // 等待后重试

  17            await new Promise(resolve => setTimeout(resolve, retryDelay * (i + 1)));

  18          }

  19        }

  20      }

  21  

  22      throw lastError;

  23    }

  24  

  25    /**

  26     * 带缓存的请求

  27     */

  28    async withCache(key, fn, options = {}) {

  29      const { cacheTime = 60000, cache = new Map() } = options;

  30  

  31      const cached = cache.get(key);

  32      if (cached && Date.now() - cached.timestamp < cacheTime) {

  33        logger.debug('使用缓存数据', { key });

  34        return cached.data;

  35      }

  36  

  37      const data = await fn();

  38      cache.set(key, {

  39        data,

  40        timestamp: Date.now(),

  41      });

  42  

  43      return data;

  44    }

  45  

  46    /**

  47     * 批量请求处理

  48     */

  49    async handleBatch(requests, options = {}) {

  50      const { stopOnError = false, showProgress = false } = options;


*第 53 页*

---

## 第 54 页

   1      const results = [];

   2  

   3      for (let i = 0; i < requests.length; i++) {

   4        try {

   5          const result = await requests[i].fn();

   6          results.push({ success: true, data: result, index: i });

   7  

   8          if (showProgress) {

   9            message.loading(`处理中... ${i + 1}/${requests.length}`);

  10          }

  11        } catch (error) {

  12          const errorResult = this.handleError(error, {

  13            showMessage: false,

  14            ...requests[i].options,

  15          });

  16          results.push({ success: false, error: errorResult, index: i });

  17  

  18          if (stopOnError) {

  19            break;

  20          }

  21        }

  22      }

  23  

  24      return {

  25        results,

  26        successCount: results.filter(r => r.success).length,

  27        errorCount: results.filter(r => !r.success).length,

  28        allSuccess: results.every(r => r.success),

  29      };

  30    }

  31  

  32    /**

  33     * 获取错误统计

  34     */

  35    getErrorStats() {

  36      const stats = {};

  37  

  38      for (const [endpoint, errors] of this.errorCount.entries()) {

  39        const recentErrors = errors.filter(

  40          time => Date.now() - time < this.errorTimeWindow

  41        );

  42  

  43        if (recentErrors.length > 0) {

  44          stats[endpoint] = {

  45            count: recentErrors.length,

  46            lastError: recentErrors[recentErrors.length - 1],

  47          };

  48        }

  49      }

  50  


*第 54 页*

---

## 第 55 页

   1      return stats;

   2    }

   3  

   4    /**

   5     * 重置错误计数

   6     */

   7    resetErrors(endpoint = null) {

   8      if (endpoint) {

   9        this.errorCount.delete(endpoint);

  10      } else {

  11        this.errorCount.clear();

  12      }

  13    }

  14  }

  15  

  16  // 创建全局单例

  17  const responseHandler = new APIResponseHandler();

  18  

  19  export const apiHandler = responseHandler;

  20  

  21  export default responseHandler;

  22  

  23  // 使用示例

  24  /**

  25   * // 简单使用

  26   * try {

  27   *   const result = await api.get('/assets');

  28   *   apiHandler.handleSuccess(result);

  29   * } catch (error) {

  30   *   apiHandler.handleError(error, { endpoint: '/assets' });

  31   * }

  32   *

  33   * // 带重试

  34   * try {

  35   *   const result = await apiHandler.withRetry(

  36   *     () => api.post('/assets', data),

  37   *     { maxRetries: 3, retryDelay: 1000 }

  38   *   );

  39   * } catch (error) {

  40   *   apiHandler.handleError(error);

  41   * }

  42   *

  43   * // 批量处理

  44   * const batchResult = await apiHandler.handleBatch([

  45   *   { fn: () => api.post('/asset1', data1) },

  46   *   { fn: () => api.post('/asset2', data2) },

  47   * ]);

  48   *

  49   * console.log(`成功: ${batchResult.successCount}, 失败: ${batchResult.errorCount}`);

  50   */


*第 55 页*

---

## 第 56 页

   1  

   2  

   3  // ===== FILE: frontend/src/utils/validation.js =====

   4  /**

   5   * 表单验证工具

   6   * 集中管理验证规则，与 constants/VALIDATION_RULES 配合使用

   7   */

   8  

   9  import { VALIDATION_RULES } from '../constants';

  10  

  11  /**

  12   * 创建必填验证规则

  13   * @param {string} message - 自定义错误消息

  14   */

  15  export const required = (message = '此字段为必填项') => ({

  16    required: true,

  17    message,

  18  });

  19  

  20  /**

  21   * 创建最小长度验证规则

  22   * @param {number} min - 最小长度

  23   * @param {string} [message] - 自定义错误消息

  24   */

  25  export const minLength = (min, message) => ({

  26    min,

  27    message: message || `至少 ${min} 个字符`,

  28  });

  29  

  30  /**

  31   * 创建最大长度验证规则

  32   * @param {number} max - 最大长度

  33   * @param {string} [message] - 自定义错误消息

  34   */

  35  export const maxLength = (max, message) => ({

  36    max,

  37    message: message || `最多 ${max} 个字符`,

  38  });

  39  

  40  /**

  41   * 创建长度范围验证规则

  42   * @param {number} min - 最小长度

  43   * @param {number} max - 最大长度

  44   * @param {string} [message] - 自定义错误消息

  45   */

  46  export const lengthRange = (min, max, message) => ({

  47    min,

  48    max,

  49    message: message || `长度应在 ${min} 到 ${max} 个字符之间`,

  50  });


*第 56 页*

---

## 第 57 页

   1  

   2  /**

   3   * 创建邮箱验证规则

   4   * @param {string} [message] - 自定义错误消息

   5   */

   6  export const email = (message) => ({

   7    type: 'email',

   8    message: message || VALIDATION_RULES.EMAIL?.PATTERN ? '请输入有效的邮箱地址' : '请输入有效的邮箱地址',

   9  });

  10  

  11  /**

  12   * 创建手机号验证规则（中国大陆）

  13   * @param {string} [message] - 自定义错误消息

  14   */

  15  export const phone = (message) => ({

  16    pattern: VALIDATION_RULES.PHONE?.PATTERN || /^1[3-9]\d{9}$/,

  17    message: message || '请输入有效的手机号码',

  18  });

  19  

  20  /**

  21   * 创建电话号码验证规则（支持手机号和座机）

  22   * @param {string} [message] - 自定义错误消息

  23   */

  24  export const telephone = (message) => ({

  25    pattern: /^1[3-9]\d{9}$|^0\d{2,3}-?\d{7,8}$/,

  26    message: message || '请输入有效的电话号码',

  27  });

  28  

  29  /**

  30   * 创建数字验证规则

  31   * @param {number} [min] - 最小值

  32   * @param {number} [max] - 最大值

  33   * @param {string} [message] - 自定义错误消息

  34   */

  35  export const number = (min, max, message) => {

  36    const rule = { type: 'number', message: message || '请输入有效的数字' };

  37    if (min !== undefined) rule.min = min;

  38    if (max !== undefined) rule.max = max;

  39    return rule;

  40  };

  41  

  42  /**

  43   * 创建正则表达式验证规则

  44   * @param {RegExp} pattern - 正则表达式

  45   * @param {string} [message] - 自定义错误消息

  46   */

  47  export const pattern = (pattern, message = '格式不正确') => ({

  48    pattern,

  49    message,

  50  });


*第 57 页*

---

## 第 58 页

   1  

   2  /**

   3   * 创建资产编码验证规则

   4   * @param {string} [message] - 自定义错误消息

   5   */

   6  export const assetCode = (message) => ({

   7    pattern: VALIDATION_RULES.ASSET_CODE?.PATTERN || /^[A-Za-z0-9-_]+$/,

   8    message: message || VALIDATION_RULES.ASSET_CODE?.PATTERN ? '只能包含字母、数字、- 和 _' : '只能包含字母、数字、- 和 _',

   9  });

  10  

  11  /**

  12   * 创建 MAC 地址验证规则

  13   * @param {string} [message] - 自定义错误消息

  14   */

  15  export const macAddress = (message) => ({

  16    pattern: /^([0-9A-Fa-f]{2}[:-]){5}([0-9A-Fa-f]{2})$/,

  17    message: message || '请输入有效的MAC地址格式',

  18  });

  19  

  20  /**

  21   * 创建 IP 地址验证规则

  22   * @param {string} [message] - 自定义错误消息

  23   */

  24  export const ipAddress = (message) => ({

  25    pattern: /^((25[0-5]|2[0-4]\d|[01]?\d\d?)\.){3}(25[0-5]|2[0-4]\d|[01]?\d\d?)$/,

  26    message: message || '请输入有效的IP地址',

  27  });

  28  

  29  /**

  30   * 创建 URL 验证规则

  31   * @param {string} [message] - 自定义错误消息

  32   */

  33  export const url = (message) => ({

  34    type: 'url',

  35    message: message || '请输入有效的URL地址',

  36  });

  37  

  38  /**

  39   * 创建自定义验证器

  40   * @param {Function} validator - 验证函数 (rule, value) => Promise | boolean

  41   * @param {string} [message] - 自定义错误消息

  42   */

  43  export const custom = (validator, message) => ({

  44    validator,

  45    message: message || '验证失败',

  46  });

  47  

  48  /**

  49   * 验证器组合函数（用于需要多个验证规则的字段）

  50   * @param  {...(Object|Array)} rules - 验证规则


*第 58 页*

---

## 第 59 页

   1   * @returns {Array} 合并后的验证规则数组

   2   */

   3  export const combine = (...rules) => {

   4    return rules.flat().filter(Boolean);

   5  };

   6  

   7  /**

   8   * 创建确认密码验证规则

   9   * @param {Function} getValue - 获取原密码值的函数 () => string

  10   * @param {string} [message] - 自定义错误消息

  11   */

  12  export const confirmPassword = (getValue, message) => ({

  13    validator: (_, value) => {

  14      if (!value) {

  15        return Promise.reject(message || '请确认密码');

  16      }

  17      if (value !== getValue()) {

  18        return Promise.reject(message || '两次输入的密码不一致');

  19      }

  20      return Promise.resolve();

  21    },

  22  });

  23  

  24  /**

  25   * 常用字段验证规则预设

  26   */

  27  export const fieldRules = {

  28    /** 用户名 */

  29    username: combine(

  30      required('请输入用户名'),

  31      lengthRange(3, 20, '用户名长度在 3 到 20 个字符之间'),

  32      pattern(/^[a-zA-Z0-9_-]+$/, '只能包含字母、数字、- 和 _')

  33    ),

  34  

  35    /** 密码 */

  36    password: combine(

  37      required('请输入密码'),

  38      minLength(6, '密码至少 6 个字符'),

  39      maxLength(32, '密码最多 32 个字符')

  40    ),

  41  

  42    /** 邮箱 */

  43    email: combine(

  44      required('请输入邮箱'),

  45      email()

  46    ),

  47  

  48    /** 手机号 */

  49    phone: combine(

  50      required('请输入手机号'),


*第 59 页*

---

## 第 60 页

   1      phone()

   2    ),

   3  

   4    /** 部门编码 */

   5    departmentCode: combine(

   6      required('请输入部门编码'),

   7      lengthRange(2, 20, '部门编码长度在 2 到 20 个字符之间'),

   8      pattern(/^[a-zA-Z0-9-_]+$/, '只能包含字母、数字、- 和 _')

   9    ),

  10  

  11    /** 部门名称 */

  12    departmentName: combine(

  13      required('请输入部门名称'),

  14      lengthRange(2, 50, '部门名称长度在 2 到 50 个字符之间')

  15    ),

  16  

  17    /** 资产名称 */

  18    assetName: combine(

  19      required('请输入资产名称'),

  20      lengthRange(1, 100, '资产名称长度在 1 到 100 个字符之间')

  21    ),

  22  

  23    /** 资产编码 */

  24    assetCode: combine(

  25      required('请输入资产编码'),

  26      assetCode()

  27    ),

  28  };

  29  

  30  export default {

  31    required,

  32    minLength,

  33    maxLength,

  34    lengthRange,

  35    email,

  36    phone,

  37    telephone,

  38    number,

  39    pattern,

  40    assetCode,

  41    macAddress,

  42    ipAddress,

  43    url,

  44    custom,

  45    combine,

  46    confirmPassword,

  47    fieldRules,

  48  };

  49  

  50  


*第 60 页*

---
