# 飞书通知实现检查与测试报告

> 检查时间：2026-07-12 ｜ 结论：**实现完整、接线正确、端到端实测通过**

## 一、实现情况检查（代码审查）

飞书通知是一套**完整且已接入启动流程**的集成，覆盖事件订阅、卡片渲染、接收人解析、定时调度、OAuth 绑定与前端适配。

| 模块 | 文件 | 状态 | 说明 |
|------|------|------|------|
| 核心 API 客户端 | `backend/modules/feishu-binding/services/feishu-client.js` | ✅ | tenant_access_token 获取+缓存（Redis→内存降级）、发文本/卡片、OAuth、按手机号/邮箱反查 open_id、`testConnection()` |
| 业务通知服务 | `backend/services/feishu-notification.service.js` | ✅ | 订阅 ~30+ 个 EventBus 事件（报废/调配/维修/盘点/领用归还/工单/招标/盘点任务/预防性维护/用户加入），渲染飞书交互卡片 |
| 接收人解析 | 同上 | ✅ | 优先级：`feishu_bindings` 表 → 手机号反查 → 租户审批/管理员兜底 |
| 定时调度 | `backend/services/feishu-scheduler.service.js` | ✅ | 每小时扫描高紧急度智能预警 + 每日 9:00 推送运营报表 |
| 启动接线 | `backend/server.js` | ✅ | 启动时调用 `initFeishuNotification()` + `startScheduler()`（均在 try/catch 中，失败不影响主服务） |
| 绑定模块 | `backend/modules/feishu-binding/`（routes/controllers/services） | ✅ | OAuth 绑定回调、绑定列表/解绑、用户信息与发消息接口 |
| 前端适配 | `frontend/src/utils/feishu.js`、`api/domains/feishu.js` | ✅ | 飞书工作台环境检测、JSAPI 桥接、安全降级 |
| 配置 | `backend/.env` | ✅ | 凭据已配置、开关已开、前端域名已配 |

**配置核对：**
- `FEISHU_APP_ID` / `FEISHU_APP_SECRET`：已配置（真实自建应用凭据）
- `FEISHU_HOST`：`https://open.feishu.cn`
- `FEISHU_NOTIFICATION_ENABLED=true`、`FEISHU_SCHEDULER_ENABLED=true`：均已开启
- `FRONTEND_URL=http://103.40.14.91:17565`：已为公网地址 → 卡片"查看详情"按钮可正常跳转 ✅
- `FEISHU_REDIRECT_URI=http://localhost:13579/...`：仍为 localhost → **仅本地可用**（见待优化项）⚠️

## 二、测试结果（实测）

测试在本沙箱进行。沙箱强制外部流量走本地 HTTP 代理，Node `axios` 需显式注入代理隧道（真实服务器直连无需此步）。

| 层级 | 测试项 | 结果 |
|------|--------|------|
| 1. 凭据+网络 | `curl` 直连飞书鉴权接口 | ✅ HTTP 200（飞书真实响应，凭据被接受） |
| 2. 生产代码路径 | `feishu-client.testConnection()` | ✅ 成功获取 `tenant_access_token`（前缀 `t-g1047c...`） |
| 3. 端到端发送 | 向 DB 绑定用户发送真实测试卡片 | ✅ 投递成功，`messageId=om_x100b6a0...` |

> 端到端发送目标：数据库中唯一绑定用户 `ou_21c034...`（name=lee，tenant=2）。该用户已在飞书客户端收到测试卡片。

## 三、当前数据状态

- 飞书绑定用户：**1 人**（tenant=2，name=lee）
- 系统活跃用户：**98 人**
- 绑定分布：tenant 2 → 1 人；其余租户 0 人

## 四、待优化项（建议）

| # | 问题 | 影响 | 建议 |
|---|------|------|------|
| 1 | 仅 1/98 用户绑定飞书 | 业务事件对未绑定用户只能走手机号反查/租户管理员兜底，若兜底人未绑定则**不发送**（仅记日志） | 为审批人、系统/资产管理员、维修工程师批量绑定飞书账号，确保关键通知可达 |
| 2 | `FEISHU_REDIRECT_URI` 为 localhost | 网页 OAuth「绑定飞书」流程仅在本地开发可用 | 改为公网回调地址（如 `http://103.40.14.91:17565/feishu-binding/callback`）并在飞书开放平台后台同步 |
| 3 | 多租户飞书凭据 | 通知按租户读 `tenant_configs` 飞书配置，tenant 2 若未单独配置则回退全局 env（已生效） | 如需各租户独立应用，补齐租户级 `feishu_config` |

## 五、复现命令

- **仅验证连通性（不发送）：**
  ```bash
  node backend/modules/feishu-binding/services/feishu-client.js  # 见 testConnection()
  ```
- **向所有已绑定用户发送测试卡片（交互确认）：**
  ```bash
  node backend/scripts/feishu-test-notification.js        # 所有租户
  node backend/scripts/feishu-test-notification.js 2      # 仅 tenant 2
  ```
- **真实服务器说明：** 生产环境直连公网，无需代理；上述脚本可直接运行。
